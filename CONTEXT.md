# Fiap.Workshop — Contexto do Projeto

> Documento vivo de contexto técnico. Atualize sempre que a arquitetura, os use cases ou as decisões de design mudarem. Última atualização: 2026-08-16 (branch `feature/22-reject-service-order`).
>
> Desde 2026-08-14 o repositório também tem um `CLAUDE.md` (raiz) com a postura esperada de qualquer assistente de IA trabalhando aqui: ser crítico por padrão em decisões de DDD/arquitetura, não só quando pedido. Leia os dois documentos juntos — `CLAUDE.md` é sobre *como* colaborar, este arquivo é sobre *o que* existe.

## 1. Visão geral

API .NET 10 seguindo **Clean Architecture** (Domain → Application → Infrastructure → Api), construída como template de workshop de pós-graduação FIAP. Expõe endpoints via **Minimal APIs** (não MVC controllers), com versionamento de API, autenticação JWT e autorização baseada em roles.

Solução: `Fiap.Workshop.slnx`

```
src/
  Fiap.Workshop.Domain          # Entidades, Value Objects, eventos de domínio — zero dependências externas
  Fiap.Workshop.Application     # Use cases, boundaries (Input/Output), interfaces (portas)
  Fiap.Workshop.Infrastructure  # EF Core, repositórios, serviços concretos (JWT, hashing, current user, dispatcher)
  Fiap.Workshop.Api             # Minimal API endpoints, DI composition root, validators, mappers HTTP↔Application
tests/
  Fiap.Workshop.UnitTests           # xUnit + Moq/NSubstitute + AutoFixture — Domain e Application isolados
  Fiap.Workshop.IntegrationTests    # xUnit + EF Core real contra SQL Server (docker-compose.tests.yml)
  Fiap.Workshop.FunctionalTests     # xUnit + Microsoft.AspNetCore.Mvc.Testing — projeto criado, SEM testes ainda
```

Regra de dependência: `Domain` não referencia nada; `Application` referencia `Domain`; `Infrastructure` referencia `Application`; `Api` referencia `Infrastructure` (e transitivamente todo o resto).

## 2. Stack e pacotes principais

- **.NET 10** / C# `latest`, `Nullable` habilitado, `TreatWarningsAsErrors=true`, `EnforceCodeStyleInBuild=true` (`Directory.Build.props`)
- **API**: Minimal APIs, `Asp.Versioning.Http` + `Asp.Versioning.Mvc.ApiExplorer` (versionamento via segmento de URL `/api/v{version}/...`; o `ApiExplorer` com `SubstituteApiVersionInUrl = true` é o que faz `{version}` virar `1` de verdade na doc gerada — sem ele, Swagger/OpenAPI mandavam o template cru e todo request pelo Swagger UI dava 404 de roteamento, corrigido em 2026-07-12), `Microsoft.AspNetCore.OpenApi` (gera o documento OpenAPI em `/openapi/v1.json`), `Swashbuckle.AspNetCore.SwaggerUI` (só a UI, em `/swagger` — trocado do Scalar em 2026-07-11, mantendo a geração do doc como estava)
- **Logging**: `Serilog.AspNetCore` (instalado em 2026-07-11) — substitui o provider padrão do `Microsoft.Extensions.Logging` via `builder.Host.UseSerilog(...)` no `Program.cs`; usa bootstrap logger + `try/catch/finally` em volta do host pra capturar falhas de startup (ex.: banco indisponível na migration automática) antes mesmo do DI terminar de montar; sink Console configurado via `appsettings.json` (`Serilog` section, `ReadFrom.Configuration`); `app.UseSerilogRequestLogging()` loga cada requisição HTTP. Todo `ILogger<T>` já injetado nos use cases continua funcionando sem mudança nenhuma — a troca foi só na composição, não no código de Application/Domain.
- **Validação**: FluentValidation (validators na camada Api, um por Request)
- **Auth**: JWT Bearer (`Microsoft.AspNetCore.Authentication.JwtBearer`), policies `AdminOnly`, `AttendantOnly` (roles `Attendant`/`Admin`) e `MechanicOnly` (roles `Mechanic`/`Admin`) — substituíram as antigas `AdminOnly`/`UserOnly` em algum ponto entre 2026-08-08 e 2026-08-11, junto com a mudança de `UserRole` (ver §3): não existe mais um role genérico `User`, só os três papéis de negócio da oficina.
- **Hashing de senha**: `BCrypt.Net-Next` (work factor 12), via `IPasswordService` (`Application.Interfaces.Services`) / `PasswordService` (Infrastructure) — ver §10 para a diferença em relação a uma iteração anterior do projeto que tinha isso como Domain Service
- **Validação de CPF/CNPJ**: pacote `Cpf.Cnpj` (namespace `CpfCnpjLibrary`), usado via `StringExtensions.StandardizeDocument`/`IsValidDocument` (`Application/Commons/StringExtensions.cs`) — ver §4 e §6
- **Persistência**: EF Core 10 + SQL Server (`Microsoft.EntityFrameworkCore.SqlServer`)
- **Testes**: xUnit, FluentAssertions, Moq, NSubstitute (ambos presentes — inconsistência a resolver, ver §9), AutoFixture
- **Erros HTTP**: `AddProblemDetails()` + `UseExceptionHandler()`

## 3. Camada de Domínio (`Fiap.Workshop.Domain`)

> ⚠️ Esta seção foi reescrita em 2026-08-08 para refletir o código real da branch `feature/3-create-user-uc`. Uma iteração anterior do projeto (registrada em commits antigos) havia desenhado `User` com Value Objects (`HashedPassword`, `Email`) e fábricas nomeadas (`Create`/`Rehydrate`); esse desenho **não existe no código atual** — ver nota em §10.

### Abstrações (`Abstractions/`)
- `AggregateRoot` (classe abstrata, **não genérica**): construtor posicional `(Guid id, DateTime createdAt, DateTime updatedAt)`; `Id`/`CreatedAt`/`UpdatedAt` com setter `protected`; lista interna de `IDomainEvent`; `RaiseDomainEvent` (protected), `GetDomainEvents()`, `ClearDomainEvents()`, `SetUpdatedAt()` (protected, `UpdatedAt = DateTime.Now` — trocado de `DateTime.UtcNow` em 2026-08-12 pra manter consistência com o resto do código, que usa `DateTime.Now` em todo lugar, ex. `CreateUserMapper`/`CreateCustomerMapper`). Até `feature/7-get-users`, `SetUpdatedAt()` existia mas **nenhum agregado a usava**; `User` é o primeiro a chamá-la, via `UpdateProfile`/`ChangePassword` (ver abaixo).
- `ValueObject`: igualdade estrutural via `GetEqualityComponents()` — hoje **sem nenhum VO concreto usando** (nenhuma classe do Domain herda dela).
- `DomainException(string message)`: exceção de domínio simples.
- `IDomainEvent`: contrato marcador (não existe `IAggregateRoot` separado).

### Agregado `User` (`Entities/User.cs`)
Propriedades: `Name`, `Password`, `Role` (`UserRole`), `Email` — todas `string`/`enum` simples com setter `private`. **Não há Value Objects.**

Um único construtor **público**, posicional: `User(Guid id, string email, string name, string password, UserRole role, DateTime createdAt, DateTime updatedAt)`. Não existem `Create`/`Rehydrate`/fábricas nomeadas, e não há validação de invariante nenhuma no construtor (nome vazio, formato de e-mail, força de senha — tudo isso, se acontece, acontece na Api, ver §6). O construtor **sempre** dispara `RaiseDomainEvent(new UserCreatedEvent(id))`, inclusive quando `DomainMappers.MapToDomain` (Infrastructure) chama `new User(...)` para reidratar um usuário lido do banco — na prática é inofensivo hoje porque só `UserRepository.AddAsync`/`Update` enfileiram os eventos pendentes no `AppDbContext` (`EnqueueEvents`); leituras (`GetByIdAsync`/`GetByEmailAsync`) descartam o evento junto com o objeto. É um ponto frágil a observar se um dia uma leitura passar a enfileirar eventos — todo `SELECT` de usuário reemitiria "usuário criado".

Dois métodos de comportamento, adicionados em `feature/8-put-users` (2026-08-12) pros use cases `UpdateUser`/`ChangePassword` (ver §4):
- `UpdateProfile(string name, string email, UserRole role)`: muta `Name`/`Email`/`Role` direto e chama `SetUpdatedAt()`.
- `ChangePassword(string newPasswordHash)`: muta `Password` direto e chama `SetUpdatedAt()`.

**Bug corrigido nesta sessão, antes de esses métodos existirem**: a primeira versão de "atualizar usuário" reconstruía um `User` inteiro via `new User(...)` só pra trocar 3 campos — e como o construtor sempre dispara `UserCreatedEvent` (parágrafo acima), isso fazia todo `Update` (mesmo um repeat idempotente) enfileirar um evento de "usuário criado" incorreto via `UserRepository.Update`'s `EnqueueEvents`. `UpdateProfile`/`ChangePassword` resolvem isso na raiz: mutam o agregado já existente em vez de reconstruí-lo, então nenhum dos dois dispara evento. É o padrão a seguir pra qualquer mutação futura de `User` — nunca `new User(...)` pra "atualizar", sempre um método de comportamento dedicado.

### `UserRole` (enum, **mudou entre 2026-08-08 e 2026-08-11**): `Admin = 0`, `Attendant = 1`, `Mechanic = 2`
Substituiu o desenho anterior (`User = 0`, `Admin = 1`) — não existe mais um role genérico `User`; os três valores mapeiam papéis reais da oficina (recepção/atendimento, mecânico, administrador). Todos os testes que fixavam `UserRole.User` foram migrados para `UserRole.Admin` (commit `14b2aac`, "test(refactor): fixing the enum"). Ver policies novas em §2/§6.

### `UserErrors` (constantes de mensagem, hoje **não usadas por nenhum use case** — sobras de uma iteração anterior)
`NameEmpty`, `NotFound`, `EmailAlreadyInUse`, `PasswordEmpty`, `PasswordTooShort`, `PasswordMissingUppercase`, `PasswordMissingLowercase`, `PasswordMissingDigit`, `PasswordHashEmpty`, `InvalidCredentials`.

### Eventos (`Events/`)
- `UserCreatedEvent(Guid UserId)` — disparado (sempre) no construtor de `User`, ver acima. Só carrega o `UserId` ("evento magro"). Ver §11 para a mecânica completa de domain events.

### Agregado `Customer` (`Entities/Customer.cs`) — primeiro agregado de negócio com pilha completa (2026-08-11)
Propriedades: `Name`, `Document`, `Email`, `Phone` — todas `string` simples, setter `private`. **Sem Value Objects, sem validação de invariante no construtor** (nem formato de CPF/CNPJ, nem unicidade) — consistente com a decisão de validação incremental (ver §9/§10): a checagem de formato/dígito verificador de CPF/CNPJ mora na Api (`CreateCustomerRequestValidator`, via `IsValidDocument()`), não no Domain. Construtor único, posicional: `Customer(Guid id, string name, string document, string email, string phone, DateTime createdAt, DateTime updatedAt)`. **Não dispara nenhum domain event** — diferente de `User` (ver §11), `Customer` não tem um `CustomerCreatedEvent` equivalente hoje.

Um método de comportamento, adicionado em `feature/10-update-customer` (2026-08-13) pro use case `UpdateCustomer` (ver §4): `UpdateProfile(string name, string email, string phone)` — muta `Name`/`Email`/`Phone` direto e chama `SetUpdatedAt()`, mesmo padrão do `User.UpdateProfile` (ver acima). **`Document` fica de fora de propósito**: decisão de negócio (não técnica) de que o CPF/CNPJ é imutável depois de cadastrado, igual a um documento de identidade real — não existe (e não é esperado que exista) um jeito de trocar o `Document` de um `Customer` já criado.

### Agregado `Vehicle` (`Entities/Vehicle.cs`) — primeiro agregado com invariante de construtor (2026-08-14, `feature/11-create-vehicle`)
Propriedades: `CustomerId`, `LicensePlate`, `Brand`, `Model`, `ManufactureYear`, `ModelYear`, `Color` — setter `private`. **Primeira entidade do projeto a ganhar validação real no construtor** desde a política de validação incremental (ver §9/§10): `ModelYear` não pode ser anterior a `ManufactureYear` (`VehicleErrors.InvalidModelYear`, lançado como `DomainException`) — decisão consciente de que isso é um fato de negócio sempre verdadeiro (não uma opinião de formatação), então pertence ao agregado, diferente de `User`/`Customer` (zero validação no construtor, tudo na Api). Formato de placa (Mercosul/antigo) **não** é validado aqui — fica na Api (`CreateVehicleRequestValidator`), mesmo racional do CPF/CNPJ em `Customer` (formato de string é responsabilidade da Api; verdade de negócio é responsabilidade do Domain). Por causa dessa mudança, `Vehicle` deixou de usar sintaxe de primary constructor (`class Vehicle(...) : AggregateRoot(...)`) e passou a ter um construtor explícito com corpo, mesmo molde de `ServiceOrderPart`/`ServiceOrderService` (únicas outras entidades que já validavam algo no construtor, mas nunca documentadas aqui em detalhe). **Não dispara domain event**, igual `Customer`.

**Achado crítico corrigido nesta mesma branch**: até então não existia nenhum handler global convertendo `DomainException` em `400` — se uma invariante de domínio estourasse sem tratamento, o cliente recebia `500` genérico do `UseExceptionHandler()` padrão em vez de um erro de negócio `400`. Isso já era um risco latente desde que `ServiceOrderPart`/`ServiceOrderService` ganharam validação no construtor (nenhum use case os instanciava ainda, então nunca tinha sido exercitado). Resolvido com `DomainExceptionHandler : IExceptionHandler` (ver §6) — vale pra `Vehicle` hoje e pra qualquer invariante de domínio futura, sem precisar de `try/catch` local em cada use case.

### `VehicleErrors` (`Errors/VehicleErrors.cs`, novo)
`InvalidModelYear` — única constante até agora.

### Agregado `Service` (`Entities/Service.cs`) — segunda entidade do projeto a validar invariante no construtor (2026-08-15, `feature/13-create-service`)
Propriedades: `Code`, `Name`, `Description`, `BasePrice` (`decimal`), `EstimatedDuration` (`short`), `IsActive` (`bool`) — setter `private`. Até esta branch, `Service` usava sintaxe de primary constructor (igual a `Vehicle` antes de `feature/11-create-vehicle`) sem nenhuma validação. **Decisão tomada nesta branch, discutida antes de implementar** (ver `CLAUDE.md` sobre postura crítica em decisões de DDD): as entidades-filhas de `ServiceOrder` no mesmo domínio conceitual de "preço/duração" (`ServiceOrderPart`/`ServiceOrderService`, ver abaixo) já validavam `UnitPrice`/`EstimatedDuration` não-negativos no construtor desde antes — deixar `Service.BasePrice`/`EstimatedDuration` sem a mesma invariante seria uma inconsistência sem justificativa de negócio (preço/duração negativos não são um fato de negócio válido em nenhum dos dois lugares). Por isso `Service` ganhou construtor explícito com corpo, lançando `DomainException` via `ServiceErrors` (`InvalidBasePrice`/`InvalidEstimatedDuration`, ambos "cannot be negative", permitindo `0`) — mesmo molde do `Vehicle` (ver acima), coberto pelo `DomainExceptionHandler` global (ver §6). Formato/obrigatoriedade de `Code`/`Name`/`Description` continuam só na Api (`CreateServiceRequestValidator`), mesmo racional do CPF/CNPJ e da placa. **Não dispara domain event**, igual `Customer`/`Vehicle`.

### `ServiceErrors` (`Errors/ServiceErrors.cs`, novo, `feature/13-create-service`)
`InvalidBasePrice`, `InvalidEstimatedDuration` — únicas constantes até agora, ambas efetivamente referenciadas pelo construtor de `Service` (diferente de `VehicleErrors.InvalidModelYear`, que existe mas está órfão — a mensagem equivalente do `Vehicle` está hardcoded inline no construtor, não referencia a classe; `ServiceErrors` segue o precedente real de `ServiceOrderErrors`, não o de `VehicleErrors`).

**`EstimatedDuration` não tem unidade documentada em lugar nenhum do código** (nem doc comment, nem nome de campo, nem validação) — é só um `short` cru, igual ao `EstimatedDuration` de `ServiceOrderService`. A convenção assumida nos dados de teste (`CreateServiceUseCaseUnitTests`/`IntegrationTests`, valor `60` pra "Oil Change") é **minutos**, mas isso é só uma escolha implícita de quem escreveu o teste, não uma regra imposta em nenhuma camada — se importar de verdade (ex. exibir pro usuário, ou comparar com `ActualDuration` real, ver item de pendência em §9), vale formalizar isso (nome do campo tipo `EstimatedDurationMinutes`, ou doc comment) antes que dois pontos do código assumam unidades diferentes sem perceber.

### Agregado `InventoryItem` (`Entities/InventoryItem.cs`) — terceira entidade do projeto a validar invariante no construtor (2026-08-15, `feature/15-create-inventory-item`)
Propriedades: `Code`, `Name`, `Description`, `QuantityOnHand`/`ReservedQuantity`/`MinimumStock` (`int`), `UnitPrice` (`decimal`), `UnitOfMeasure` (enum), `IsActive` (`bool`) — setter `private`. Mesma decisão tomada em `Service` (ver acima), aplicada de novo por consistência com o precedente agora estabelecido em quatro lugares (`Vehicle`, `Service`, `ServiceOrderPart`, `ServiceOrderService`): `InventoryItem` ganhou construtor explícito com corpo, validando **cinco** invariantes via `DomainException`/`InventoryItemErrors`: `QuantityOnHand`/`ReservedQuantity`/`MinimumStock`/`UnitPrice` não-negativos, mais `ReservedQuantity <= QuantityOnHand` (a regra central do próprio `InventoryItem`, já prevista no backlog pro futuro `Reserve()`/`ReleaseReservation()`/`Commit()` do ticket 20 — "não posso reservar mais do que tenho" — validada aqui como defesa em profundidade mesmo antes de esses métodos existirem, já que `CreateInventoryItem` sempre cria com `ReservedQuantity = 0`). **Não dispara domain event**, igual `Customer`/`Vehicle`/`Service`.

**Efeito colateral em testes existentes**: os testes de `InventoryItem`/mapper que já existiam antes desta branch (`InventoryItemUnitTests`, `InventoryItemMapperUnitTests`) geravam `QuantityOnHand`/`ReservedQuantity`/`MinimumStock` via `_fixture.Create<int>()` independentes — como o gerador de `int` do AutoFixture não garante `ReservedQuantity <= QuantityOnHand`, dois desses testes passaram a falhar assim que a invariante foi adicionada (`DomainException` no construtor). Corrigido trocando pra valores fixos consistentes (`100`/`10`/`5`, mesmo padrão já usado em `InventoryItemRepositoryTests`) — vale lembrar disso se outro agregado ganhar uma invariante cruzando dois campos numéricos gerados via fixture independente.

### `InventoryItemErrors` (`Errors/InventoryItemErrors.cs`, novo, `feature/15-create-inventory-item`)
`InvalidQuantityOnHand`, `InvalidReservedQuantity`, `ReservedQuantityExceedsQuantityOnHand`, `InvalidMinimumStock`, `InvalidUnitPrice`, `InsufficientStock` (novo, `feature/20-add-budget`) — todas referenciadas (as cinco primeiras pelo construtor de `InventoryItem`, `InsufficientStock` por `Reserve(quantity)` abaixo), mesmo precedente real do `ServiceErrors`/`ServiceOrderErrors` (não o de `VehicleErrors`, órfão). `InsufficientStock` é conceitualmente diferente de `ReservedQuantityExceedsQuantityOnHand` (construção/estado válido do agregado vs. uma operação específica não exceder o disponível) — constantes separadas de propósito, como já estava planejado.

### `InventoryItem.Reserve` (2026-08-15, `feature/20-add-budget`, ticket 20)

`Reserve(int quantity)`: lança `DomainException(InventoryItemErrors.InsufficientStock)` se `quantity > QuantityOnHand - ReservedQuantity`, senão incrementa `ReservedQuantity` e chama `SetUpdatedAt()`. Não valida `quantity <= 0` diretamente — confia que o chamador (`AddBudgetUseCase`, ver §4) só invoca com uma quantidade já validada como positiva pelo construtor de `ServiceOrderPart` (`ServiceOrderErrors.InvalidQuantity`), evitando duplicar a mesma invariante em dois agregados.

### `InventoryItem.CommitReservation` (2026-08-16, `feature/21-approve-service-order`, ticket 21)

`CommitReservation(int quantity)`: lança `DomainException(InventoryItemErrors.CommitQuantityExceedsReservedQuantity)` se `quantity > ReservedQuantity`, senão debita **os dois** contadores (`QuantityOnHand -= quantity` e `ReservedQuantity -= quantity`) e chama `SetUpdatedAt()`. É o segundo passo do ciclo de vida de uma reserva de estoque começado por `Reserve` (ver acima): `Reserve` só promete (`ReservedQuantity` sobe, `QuantityOnHand` intocado); `CommitReservation` efetiva a baixa real quando o orçamento é aprovado — chamado por `ApproveServiceOrderUseCase` (ver §4), uma vez por `ServiceOrderPart` da OS.

### `InventoryItem.ReleaseReservation` (2026-08-16, `feature/22-reject-service-order`, ticket 22)

`ReleaseReservation(int quantity)`: lança `DomainException(InventoryItemErrors.ReleaseQuantityExceedsReservedQuantity)` se `quantity > ReservedQuantity`, senão decrementa **só** `ReservedQuantity` (`QuantityOnHand` intocado) e chama `SetUpdatedAt()` — é o terceiro desfecho possível (alternativo ao `CommitReservation`) de uma reserva feita por `Reserve`: se o orçamento é reprovado em vez de aprovado, a promessa de reserva é desfeita sem nunca ter debitado o estoque de verdade. **Decisão discutida antes de implementar** (ver `CLAUDE.md`): o backlog original não previa validação aqui (só "decrementa `ReservedQuantity`"), mas isso deixaria `ReleaseReservation` como a única das três operações de reserva (`Reserve`/`CommitReservation`/`ReleaseReservation`) sem proteção de invariante — usuário confirmou adicionar a mesma checagem por consistência, mesmo sem um caminho de chamada conhecido hoje que a violaria (o único chamador, `RejectServiceOrderUseCase`, sempre passa quantities vindas de `ServiceOrderPart`s já reservados pelo próprio `AddBudget`). Chamado por `RejectServiceOrderUseCase` (ver §4), uma vez por `ServiceOrderPart` da OS.

### Agregado `ServiceOrder` (`Entities/ServiceOrder.cs`) — primeiro use case exposto em `feature/17-create-service-order` (2026-08-15)
Propriedades: `CustomerId`, `VehicleId`, `CreatedBy`, `Status` (`ServiceOrderStatus`, default `Received`), `ProblemDescription`, `DiagnoseDescription` (opcional), `OdometerReading`, `Discount`/`Subtotal`/`Total` (default `0`), `OpenedAt`, `ClosedAt` (opcional) — setter `private`. Ainda usa sintaxe de primary constructor com parâmetros opcionais (`status`/`diagnoseDescription`/`discount`/`subtotal`/`total`/`closedAt` todos com default), **sem nenhuma validação de invariante no construtor** — diferente de `Vehicle`/`Service`/`InventoryItem`, mas consistente com a política incremental (ver §9/§10): as invariantes reais de `ServiceOrder` (transição de status válida, desconto dentro do subtotal) só fazem sentido quando os métodos que as violam existirem (`StartDiagnosis`/`AddBudget`/`Approve`/etc., tickets 19-24, ainda não implementados) — `ServiceOrderErrors` já tem as mensagens prontas (`InvalidStatusTransition`, `InvalidDiscount`, `OrderClosed`) esperando por eles. Tem três coleções de filhos do mesmo agregado (`Parts`/`Services`/`StatusHistory`, cada um com seu próprio `Errors/ServiceOrderErrors.cs` compartilhado), todas vazias na criação (`AddParts`/`AddServices`/`AddStatusHistory` existem mas `CreateServiceOrder` não os chama — "OS nasce sem orçamento", decisão já fechada no backlog). `ServiceOrderPart`/`ServiceOrderService` já validam `Quantity`/`UnitPrice`/`EstimatedDuration` no construtor desde antes desta branch (únicas entidades com essa validação antes de `Vehicle`/`Service`/`InventoryItem`, ver notas acima). **Não dispara domain event**, igual `Customer`/`Vehicle`/`Service`/`InventoryItem`.

**Decisão discutida e revertida nesta branch, antes de implementar** (ver `CLAUDE.md` sobre postura crítica): o backlog original (`docs/backlog-proximas-tarefas.md`, ticket 17) previa um campo `Number` (int sequencial, tipo "OS #1042") gerado por `SEQUENCE`/`IDENTITY` no SQL Server. Investigado e descartado — **`ServiceOrder` **removeu** o campo `Number`, usa só `Id` (Guid) como identificador**. Dois motivos:
1. Qualquer forma de gerar um `Number` sequencial de verdade (sem gaps, coordenado entre múltiplas instâncias da API) exige uma autoridade central — só o banco serve esse papel aqui (não há Redis/coordenador distribuído no projeto). Gerar um "sequencial" só em C# teria condição de corrida entre instâncias concorrentes.
2. A forma mais simples de fazer o banco gerar esse valor (coluna `IDENTITY`/`DEFAULT NEXT VALUE FOR ...`) colide com o padrão que **todo** use case de criação já segue neste projeto: construir a entidade em memória → persistir → devolver o DTO montado a partir dessa **mesma instância em memória**, sem reler do banco depois do commit (`CreateVehicle`/`CreateService`/`CreateInventoryItem`, ver §4). Um `Number` gerado pelo banco só existiria de fato na linha persistida, não no objeto em memória usado para montar a resposta — exigiria inventar um mecanismo de releitura pós-commit só para este campo.
3. A alternativa que preservaria o `Number` sem quebrar o padrão (buscar o próximo valor de uma `SEQUENCE` via um método de repositório **antes** de construir o agregado — mesma categoria de chamada que `GetByIdAsync`/`ExistsWithCodeAsync` já fazem hoje) foi levantada e rejeitada pelo usuário por ainda envolver uma consulta ao banco só para obter o número.

Decisão final: sem esse identificador curto/humano-legível, `Id` (Guid) já cobre unicidade — a referência "OS 1042" não é um requisito confirmado do negócio. Se um dia isso importar (impressão de ordem de serviço, atendimento por telefone), reabrir a discussão com a opção 3 acima como caminho mais consistente com o resto do projeto.

`ServiceOrderPart`/`ServiceOrderService`/`ServiceOrderStatusHistory` deixaram de ser filhos permanentemente vazios do agregado nesta e na branch anterior (ver `AddBudget`/`StartDiagnosis` abaixo).

### `ServiceOrder.StartDiagnosis` — primeiro método de transição de status (2026-08-15, `feature/19-start-diagnosis`, ticket 19)

`StartDiagnosis(string diagnoseDescription, Guid changedBy)`: valida `Status == ServiceOrderStatus.Received` (senão `DomainException(string.Format(ServiceOrderErrors.InvalidStatusTransition, Status, ServiceOrderStatus.Diagnosing))`), seta `DiagnoseDescription`, muda `Status` para `Diagnosing`, chama `SetUpdatedAt()` — mesmo molde de `User.UpdateProfile`/`Vehicle`/`Service` (validação de invariante lançando `DomainException`, capturada pelo `DomainExceptionHandler` global, ver §6).

**Decisão tomada durante a implementação, antes de codar** (ver `CLAUDE.md` sobre postura crítica): o backlog original (ticket 19) previa só `StartDiagnosis(string diagnoseDescription)`, sem tocar em `StatusHistory`. Like `Parts`/`Services`, `StatusHistory` já é modelado no agregado e já é exposto pelo `GetServiceOrder` desde `feature/18` — mas nenhum ticket do backlog (19 a 24) previa populá-lo. Deixar como estava faria `StatusHistory` ficar permanentemente vazio em produção: infraestrutura modelada e exposta, nunca escrita. Discutido com o usuário, que optou por já estabelecer o padrão nesta primeira transição em vez de repetir a lacuna nos próximos cinco use cases (tickets 20-24, todos vão seguir o mesmo molde: capturar `Status` anterior, mudar `Status`, adicionar um `ServiceOrderStatusHistory(Guid.NewGuid(), Id, previousStatus, Status, changedBy, DateTime.Now)` via `_statusHistory.Add(...)`). `changedBy` (quem fez a transição) passou a ser parâmetro do método — vem de `ICurrentUserService` na Api, não do corpo da requisição, mesmo racional do `CreatedBy` em `CreateServiceOrder`.

**Achado de infra durante a implementação**: adicionar um filho novo (`ServiceOrderStatusHistory`) a uma `ServiceOrder` já persistida expôs de imediato o bug já diagnosticado no §5 (o mesmo que já bloqueava o ticket 20) — corrigido nesta branch, mas só para `StatusHistory`. **Estendido para `Parts`/`Services` em `feature/20-add-budget`** (ver §5) — o gap está totalmente fechado agora.

### `ServiceOrder.AddBudget` — segundo método de transição de status (2026-08-15, `feature/20-add-budget`, ticket 20)

`AddBudget(IEnumerable<ServiceOrderService> services, IEnumerable<ServiceOrderPart> parts, Guid changedBy)`: valida `Status == ServiceOrderStatus.Diagnosing` (senão `DomainException(InvalidStatusTransition)`, mesmo molde do `StartDiagnosis`), calcula `Subtotal = Σ(UnitPrice × Quantity)` de todos os serviços e peças recebidos, adiciona os filhos via `AddServices`/`AddParts` (reaproveitados, não duplica a lógica de `_services.AddRange`/`_parts.AddRange`), seta `Subtotal`/`Total = Subtotal` (**sem `Discount`** — ver decisão abaixo), muda `Status` para `AwaitingApproval`, registra a transição em `StatusHistory` (mesmo padrão do `StartDiagnosis`) e chama `SetUpdatedAt()`.

`ServiceOrderPart`/`ServiceOrderService` que entram aqui já vêm "fotografados" do catálogo (`Name`/`Description`/`UnitPrice` copiados de `InventoryItem`/`Service` no momento da criação, não uma referência viva) — é o `AddBudgetUseCase` (ver §4) quem monta esses objetos antes de chamar `AddBudget`, não o método do agregado; a instanciação em si foi extraída pro `Mapper/AddBudgetMapper.cs` da Application (ver §4), não fica inline no use case.

**Decisão revista pelo usuário logo após a primeira versão desta branch**: `AddBudget` originalmente recebia `decimal discount` e já validava `0 <= discount <= Subtotal` (`ServiceOrderErrors.InvalidDiscount`), setando `Total = Subtotal - Discount` ali mesmo. Removido — "montar o orçamento" (quais peças/serviços entram, quanto custam) e "aplicar um desconto" são decisões de negócio distintas, com potencial de ator/timing diferentes (o backlog fechado hoje não especifica quem/quando aplica desconto), e misturar as duas no mesmo método acoplava algo que ainda não foi desenhado. `Discount` permanece no agregado (propriedade já existe desde `CreateServiceOrder`, sempre `0` por padrão) e `Total = Subtotal` enquanto nenhum desconto for aplicado. `ServiceOrderErrors.InvalidDiscount` volta a ficar órfã (sem consumidor) — igual estava antes desta branch — esperando por um futuro use case de desconto, ainda não modelado nem no backlog (nenhum dos tickets 21-24 cobre isso). Não inventar esse desenho agora; só registrar o gap.

### `ServiceOrder.Approve` — terceiro método de transição de status (2026-08-16, `feature/21-approve-service-order`, ticket 21)

`Approve(Guid changedBy)`: valida `Status == ServiceOrderStatus.AwaitingApproval` (senão `DomainException(InvalidStatusTransition)`, mesmo molde do `StartDiagnosis`/`AddBudget`), muda `Status` para `InProgress`, registra a transição em `StatusHistory`, chama `SetUpdatedAt()` — mais simples que `AddBudget`, não toca em `Parts`/`Services`/`Subtotal`/`Total` (o orçamento já foi fechado, `Approve` só registra a decisão do atendente/cliente e libera a execução). Quem debita o estoque de verdade (`InventoryItem.CommitReservation`, ver acima) é o `ApproveServiceOrderUseCase` (ver §4) — mesmo racional do `AddBudget`/`Reserve`: `ServiceOrder` não mexe em `InventoryItem` diretamente (agregados diferentes), a orquestração fica na Application.

### `ServiceOrder.Reject` — quarto método de transição de status (2026-08-16, `feature/22-reject-service-order`, ticket 22)

`Reject(Guid changedBy)`: valida `Status == ServiceOrderStatus.AwaitingApproval` (senão `DomainException(InvalidStatusTransition)`, mesmo molde do `Approve`), muda `Status` para `Cancelled`, registra a transição em `StatusHistory`, chama `SetUpdatedAt()` — estrutura idêntica ao `Approve` (mesma pré-condição de status, o caminho "aprovar" e "reprovar" partem do mesmo estado `AwaitingApproval`), só o status de destino muda. Quem libera a reserva de estoque de verdade (`InventoryItem.ReleaseReservation`, ver acima) é o `RejectServiceOrderUseCase` (ver §4), mesmo racional do `Approve`/`CommitReservation`.

## 4. Camada de Aplicação (`Fiap.Workshop.Application`)

> ⚠️ Reescrita em 2026-08-08 (junto com §3), atualizada em 2026-08-11 com a chegada de `LoginUser` e `CreateCustomer`, em 2026-08-12 com `GetCustomer` e `GetUsers` (branch `feature/7-get-users`), no mesmo dia com `UpdateUser` e `ChangePassword` (branch `feature/8-put-users`), em 2026-08-13 com `DeleteUser` (branch `feature/9-user-delete`), no mesmo dia com `UpdateCustomer` (branch `feature/10-update-customer`), em 2026-08-14/15 com `CreateVehicle`/`GetVehicle` (branches `feature/11-create-vehicle`/`feature/12-get-vehicle`) — primeiro agregado de negócio além de `User`/`Customer` a ganhar Application+Api — em 2026-08-15 com `CreateService` (branch `feature/13-create-service`), segundo agregado a ganhar invariante validada no construtor (ver §3), no mesmo dia com `GetServices` (branch `feature/14-get-services`), e no mesmo dia com `CreateInventoryItem` (branch `feature/15-create-inventory-item`), terceiro agregado a ganhar invariante validada no construtor, e em 2026-08-15 com `GetInventoryItems` (branch `feature/16-get-inventory-item`), fechando o mesmo par Create+GetAll que `Service` já tinha, no mesmo dia com `CreateServiceOrder` (branch `feature/17-create-service-order`) — primeiro use case de `ServiceOrder`, abrindo a máquina de estados que os tickets 18-24 vão completar — em 2026-08-15 com `GetServiceOrder` (branch `feature/18-get-service-order`), em 2026-08-15 com `StartDiagnosis` (branch `feature/19-start-diagnosis`, ticket 19) — primeiro método de transição de status de `ServiceOrder`, ver §3, em 2026-08-15 com `AddBudget` (branch `feature/20-add-budget`, ticket 20) — primeiro use case a orquestrar dois agregados (`ServiceOrder` + `InventoryItem`) na mesma transação, em 2026-08-16 com `ApproveServiceOrder` (branch `feature/21-approve-service-order`, ticket 21), e no mesmo dia com `RejectServiceOrder` (branch `feature/22-reject-service-order`, ticket 22). `UpdateEmailUseCase` (o scaffold antigo, só `Email`) continua sem existir — foi substituído em espírito pelo `UpdateUser` (Name/Email/Role) descrito abaixo.
>
> ⚠️ **A pasta `UseCases/` foi reorganizada em 2026-08-15**: cada use case agora vive dentro de uma subpasta do agregado a que pertence (`UseCases/Users/CreateUser/`, `UseCases/Customers/CreateCustomer/`, `UseCases/Vehicles/CreateVehicle/`, etc.) em vez de todos soltos direto em `UseCases/`. Namespaces acompanham a pasta (`Fiap.Workshop.Application.UseCases.Users.CreateUser`, etc.). Motivo: com `Service`/`InventoryItem`/`ServiceOrder` chegando (`ServiceOrder` sozinho vai ter uns 8 use cases, ver `docs/backlog-proximas-tarefas.md`), a pasta plana ia virar uma lista enorme sem agrupamento. **A subpasta é plural (`Users`/`Customers`/`Vehicles`), não singular** — tentativa inicial com singular (`UseCases/User/`, `UseCases/Customer/`, `UseCases/Vehicle/`) quebrou o build: um arquivo dentro de `namespace ...UseCases.Customer.CreateCustomer.Mapper` que referencia o tipo `Customer` (`Domain.Entities.Customer`) sem qualificar dá `CS0118 "Customer" é um namespace, mas é usado como um tipo` — o compilador prioriza o namespace irmão de mesmo nome sobre o tipo trazido por `using Fiap.Workshop.Domain.Entities;`. Plural evita a colisão de propósito (`Interfaces/UseCases/` continua singular e sem subpasta, porque nada lá referencia o tipo de domínio sem qualificar). Os caminhos de arquivo citados abaixo neste documento já refletem a nova estrutura — se você estiver lendo um commit anterior a essa data, os caminhos eram um nível mais raso (sem a subpasta do agregado).

Padrão de use case: **Input (record) → UseCase (classe) → Output (classe genérica com Result/Errors)**, sem MediatR — injeção direta de interfaces de use case.

### `Commons/Output.cs`
Classe de retorno padrão de todos os use cases: `IsValid`, `Messages`, `ErrorMessages`, `Result` (object, lido via `GetResult<T>()`), métodos `AddResult`, `AddMessage`, `AddErrorMessage(s)`.

### Use cases existentes (vinte e um hoje: `CreateUser`, `LoginUser`, `CreateCustomer`, `GetCustomer`, `GetUsers`, `UpdateUser`, `ChangePassword`, `DeleteUser`, `UpdateCustomer`, `CreateVehicle`, `GetVehicle`, `CreateService`, `GetServices`, `CreateInventoryItem`, `GetInventoryItems`, `CreateServiceOrder`, `GetServiceOrder`, `StartDiagnosis`, `AddBudget`, `ApproveServiceOrder`, `RejectServiceOrder`)

Cada use case tem uma pasta `Boundaries/` (Input) e `Mapper/` (mapeamento entre camadas).

#### `UseCases/Users/CreateUser/`

`CreateUserUseCase` (interface `ICreateUserUseCase`), input `CreateUserInput(CorrelationId, Name, Email, Password, Role)`:
1. Checa e-mail único via `IUserRepository.ExistsWithEmailAsync` — se já existe, loga `LogWarning` e retorna `Output` com erro (`"User with email {Email} already exists."`), sem tocar no repositório.
2. Hasheia a senha via `IPasswordService.Hash(input.Password)`.
3. Monta o `User` via `input.MapToDomain(passwordHash)` (usando `input.Role` direto, sem lógica extra) e persiste (`AddAsync` + `UnitOfWork.CommitAsync`).
4. Se o commit falhar (`CommitAsync` retorna `false`), loga `LogError` e retorna erro (`"Error saving user with email {Email}."`).
5. Em sucesso, `output.AddResult(user.MapToDto())` — **mudou em relação à versão anterior deste documento**: hoje devolve `UserResponse` (DTO), não a entidade `User` crua.

Recebe `ILogger<CreateUserUseCase>` no construtor. Cobertura de teste: 3 cenários unitários (sucesso, e-mail duplicado, falha ao salvar) em `CreateUserUseCaseUnitTests`, usando o helper `LoggerTestBase<TCategory>` (`tests/Fiap.Workshop.UnitTests/Common/`) para verificar `ILogger.Log(...)` sem tropeçar no fato de `LogWarning`/`LogError` serem extension methods (Moq não consegue mockar/verificar extension methods diretamente — é preciso verificar o `ILogger.Log<TState>` real por trás). Mais cenários de integração (sucesso, e-mail duplicado) em `CreateUserUseCaseIntegrationTests`, batendo no SQL Server real via `DatabaseFixture`.

**Mapper (`UseCases/CreateUser/Mapper/CreateUserMapper.cs`)**
- `MapToDomain(this CreateUserInput input, string passwordHash)`: `new User(Guid.NewGuid(), input.Email, input.Name, passwordHash, input.Role, DateTime.Now, DateTime.Now)`.
- `MapToDto(this User user)`: `new UserResponse(user.Id, user.Name, user.Email, user.Role.ToString())` — usado por `CreateUserUseCase` (ver acima) e por `GetUsersMapper` (ver `UseCases/GetUsers/` abaixo).

**Bug corrigido em 2026-08-12**: até então, `MapToDomain` usava **`input.CorrelationId` como `Id` do `User`**, em vez de gerar um `Guid` novo — diferente do padrão usado em `CreateCustomer` (`Guid.NewGuid()`, ver abaixo). Isso quebrou em produção: um cliente que reenvia a mesma requisição com o mesmo `CorrelationId` (timeout, duplo clique, retry) faz a segunda tentativa colidir com a `PRIMARY KEY` da tabela `Users`, gerando um `DbUpdateException` (violação de PK) em vez do erro amigável de "e-mail já existe" — porque o conflito acontece no `Id`, não no `Email`, então `ExistsWithEmailAsync` não pega o caso antes do `INSERT`. Corrigido trocando para `Guid.NewGuid()`, alinhando com `CreateCustomer`.

#### `UseCases/Users/LoginUser/` (novo desde a última reescrita deste documento)

`LoginUserUseCase` (interface `ILoginUserUseCase`), input `LoginUserInput(CorrelationId, Email, Password)`:
1. Busca o usuário por e-mail (`IUserRepository.GetByEmailAsync`) — se não existir, loga `LogWarning` e retorna erro `"User not found."`.
2. Verifica a senha via `IPasswordService.Verify(input.Password, user.Password)` — se inválida, loga `LogWarning` e retorna erro `"Invalid password."`.
3. Gera o token via `IJwtService.GenerateToken(user)` (assinatura mudou, ver Interfaces abaixo) e devolve o token cru (`string`) via `output.AddResult(token)` — não há um `LoginResponse`/DTO envolvido aqui, apesar de `LoginResponse` existir em `DTOs/Users/`.

Sem mapper próprio na Application (o `LoginUserInput` é montado direto pelo `LoginUserMapper` da Api, ver §6). Cobertura: unitários (`LoginUserUseCaseUnitTests`) e de integração (`LoginUserUseCaseIntegrationTests`, seedando um usuário via `ICreateUserUseCase` de verdade antes de logar).

#### `UseCases/Customers/CreateCustomer/` (novo, branch atual `feature/5-create-customer-uc`)

`CreateCustomerUseCase` (interface `ICreateCustomerUseCase`), input `CreateCustomerInput(CorrelationId, Name, Document, Email, Phone)` — o próprio `init` de `Document` já normaliza o valor via `StringExtensions.StandardizeDocument()` (remove pontuação, formata conforme CPF/11 dígitos ou CNPJ/14 dígitos; lança `ArgumentException` para outro tamanho):
1. Checa documento único via `ICustomerRepository.AnyAsync(input.Document, ...)` — se já existe, loga `LogWarning` e retorna erro `"Customer with the provided document already exists."`, sem tocar no repositório.
2. Monta o `Customer` via `input.MapToDomain()` (gera um novo `Guid` para o `Id` — diferente do `CreateUser`, que reusa `CorrelationId`) e persiste (`AddAsync` + `UnitOfWork.CommitAsync`).
3. Se o commit falhar, loga `LogWarning` e retorna erro `"Failed to save customer to the database."`.
4. Em sucesso, `output.AddResult(customer.MapToDto())` — devolve `CustomerResponse(Id, Name, Phone)` (**não inclui `Document`/`Email`** no DTO de resposta).

Validação de **formato/dígito verificador de CPF/CNPJ não acontece aqui** — só a normalização de pontuação. A validação de fato (`IsValidDocument()`) mora em `CreateCustomerRequestValidator` na Api (ver §6); o use case em si aceita qualquer string cujo tamanho normalizado seja 11 ou 14 caracteres. Cobertura: unitários (`CreateCustomerUseCaseUnitTests` — sucesso, documento duplicado, falha ao salvar) e de integração (`CreateCustomerUseCaseIntegrationTests`, adicionada em 2026-08-11 — sucesso persistindo e lendo de volta via `ICustomerRepository.GetByIdAsync`, e falha por documento duplicado; usa um gerador de CPF com dígito verificador válido em `TestData.Document()` para não depender de string fixa).

**Mapper (`UseCases/CreateCustomer/Mapper/CreateCustomerMapper.cs`)**
- `MapToDomain(this CreateCustomerInput input)`: `new Customer(Guid.NewGuid(), input.Name, input.Document, input.Email, input.Phone, DateTime.Now, DateTime.Now)`.
- `MapToDto(this Customer customer)`: `new CustomerResponse(customer.Id, customer.Name, customer.Phone)`.

#### `UseCases/Customers/GetCustomer/` (existia antes desta branch, nunca tinha sido documentado aqui)

`GetCustomerUseCase` (interface `IGetCustomerUseCase`), input `GetCustomerInput` — **não é um `record`, é um `readonly struct`** (`CorrelationId`, `CustomerId`); `DeleteUserInput` (ver abaixo) seguiu o mesmo desenho depois, então já são dois boundaries `readonly struct` no projeto, ambos "por id apenas" (sem payload de negócio) — os demais continuam `record`/classe:
1. Busca por id via `ICustomerRepository.GetByIdAsync(input.CustomerId, ...)` — se não encontrar, loga `LogWarning` e retorna erro `"Unable to find customer"`.
2. Em sucesso, `output.AddResult(customer.MapToDto())`.

**Sem mapper próprio** — reaproveita `CreateCustomerMapper.MapToDto` (`UseCases/CreateCustomer/Mapper/`) em vez de ter um `GetCustomerMapper.cs` dedicado; diferente de `GetUsers` (abaixo), que tem mapper próprio. Cobertura: unitários (`GetCustomerUseCaseUnitTests`) e integração (`GetCustomerUseCaseIntegrationTests` — cria um customer de verdade via `ICreateCustomerUseCase` e busca de volta; caso "não existe" usa um `Guid.NewGuid()` que nunca foi inserido, não depende de a tabela estar vazia).

#### `UseCases/Users/GetUsers/` (novo, branch atual `feature/7-get-users`)

`GetUsersUseCase` (interface `IGetUsersUseCase`), input é só um `Guid correlationId` solto — **não tem boundary/record próprio**, diferente de todos os outros use cases:
1. Busca todos via `IUserRepository.GetAllAsync(...)` — sem filtro, sem paginação, devolve a tabela `Users` inteira.
2. Se a lista vier vazia, loga `LogWarning` e `output.AddResult(Array.Empty<User>())`.
3. Se houver usuários, `output.AddResult(users.MapToDto())` (`IEnumerable<UserResponse>`, via `GetUsersMapper`).
4. Em ambos os casos `output.IsValid` fica `true` (o `AddResult` sempre marca válido) — **não existe um caminho de erro real** neste use case; "lista vazia" é sucesso, só logado como warning.

⚠️ **Inconsistência de tipo em `Output.Result`**: o passo 2 devolve `Array.Empty<User>()` (entidades de domínio) e o passo 3 devolve `IEnumerable<UserResponse>` (DTO) — dois formatos diferentes de payload dependendo de a tabela estar vazia ou não. Como `Output.Result` é `object?` (sem tipo genérico), isso não gera erro de compilação, mas quem serializa a resposta (o endpoint HTTP, ver §6) devolve um JSON com forma diferente em cada caso (ex.: `Password`/hash exposto no caminho vazio, já que seria a entidade `User` crua — hoje inofensivo só porque o array está sempre vazio nesse caminho, mas é uma armadilha se alguém copiar esse padrão em outro use case). Vale corrigir para `output.AddResult(Enumerable.Empty<UserResponse>())` (ou `users.MapToDto()` já aceita lista vazia direto, tornando o `if` desnecessário).

**Mapper (`UseCases/GetUsers/Mapper/GetUsersMapper.cs`)**
- `MapToDto(this IEnumerable<User> users)`: `users.Select(x => x.MapToDto())` — reaproveita o `User.MapToDto()` de `CreateUserMapper` (ver acima) por extensão, não duplica o mapeamento campo a campo.

Cobertura: unitários (`GetUsersUnitTests` — sucesso com usuários via `AutoFixture.CreateMany<User>()`, e sucesso+log quando a lista vem vazia, usando `LoggerTestBase<GetUsersUseCase>` pra verificar o `LogWarning`) e integração (`GetUsersUseCaseIntegrationTests` — cria um usuário via `ICreateUserUseCase` e confere que ele aparece no resultado). **Não existe cenário de integração "lista vazia"**: como `DatabaseFixture` é compartilhado por toda a collection `"Integration"` (`ICollectionFixture`, sem reset entre testes) e `GetAllAsync` lê a tabela inteira sem filtro, não há como garantir a tabela vazia de forma confiável entre classes de teste diferentes — decisão consciente de só cobrir esse caminho no unit test (mockado).

#### `UseCases/Users/UpdateUser/` (novo, branch atual `feature/8-put-users`)

`UpdateUserUseCase` (interface `IUpdateUserUseCase`), input `UpdateUserInput(CorrelationId, UserId, Name, Email, Role)` — **sem `Password`**, decisão consciente (ver `ChangePassword` abaixo, troca de senha é um fluxo separado):
1. Busca o usuário por id (`GetByIdAsync`) — não existe → `LogWarning` + erro `"Unable to find user"`.
2. **Só checa e-mail duplicado se o e-mail estiver mudando** (`if (!string.Equals(existing.Email, input.Email, ...))`) — comparando contra o estado **atual** buscado no banco a cada chamada, não contra um snapshot antigo. Isso é o que torna a rota idempotente: uma segunda chamada idêntica não colide com o próprio e-mail que ela mesma já gravou na primeira chamada. Se checasse sempre, incondicionalmente, um retry do mesmo `PUT` falharia com "e-mail já existe" na segunda vez.
3. Se o e-mail mudou e já pertence a outro usuário, `LogWarning` + erro `"User with email {Email} already exists."` (mesma mensagem do `CreateUser`).
4. Aplica `user.UpdateProfile(input.Name, input.Email, input.Role)` (método de comportamento no `User`, ver §3 — **não reconstrói o agregado**) e persiste (`Update` + `UnitOfWork.CommitAsync`).
5. Falha no commit → `LogError` + erro `"Error updating user with id {UserId}."`.
6. Sucesso → `output.AddResult(user.MapToDto())`, reaproveitando `CreateUserMapper.MapToDto` (sem mapper próprio — o `UpdateUserMapper` que existiu brevemente foi removido; a "tradução" de input pra chamada de domínio é só `user.UpdateProfile(...)` direto no use case).

Cobertura: unitários (`UpdateUserUseCaseUnitTests` — 5 cenários: sucesso sem mudar e-mail, sucesso mudando e-mail livre, falha usuário não existe, falha e-mail já em uso, falha ao salvar) e integração (`UpdateUserUseCaseIntegrationTests` — persiste e relê via `IUserRepository.GetByIdAsync`, falha usuário não existe, falha e-mail duplicado entre dois usuários seedados). Domínio: `UserUnitTests.User_ShouldUpdateProfile_WhenUpdateProfileIsCalled` confirma que `Name`/`Email`/`Role` mudam, `UpdatedAt` avança, e **nenhum evento novo é disparado**.

#### `UseCases/Users/ChangePassword/` (novo, branch atual `feature/8-put-users`)

`ChangePasswordUseCase` (interface `IChangePasswordUseCase`), input `ChangePasswordInput(CorrelationId, UserId, CurrentPassword, NewPassword)` — desenhado como **troca self-service** (o usuário prova que é ele mesmo enviando a senha atual), não reset administrativo:
1. Busca o usuário por id — não existe → `LogWarning` + erro `"Unable to find user"`.
2. Verifica a senha atual via `IPasswordService.Verify(input.CurrentPassword, user.Password)` — inválida → `LogWarning` + erro `"Invalid current password"`, **sem persistir nada** (`Update` nunca é chamado nesse caminho).
3. Hasheia a nova senha (`IPasswordService.Hash`) e aplica via `user.ChangePassword(newPasswordHash)` (método de comportamento, ver §3 — mesma lógica de "mutar, não reconstruir" do `UpdateProfile`).
4. Persiste (`Update` + `UnitOfWork.CommitAsync`) — falha → `LogError` + erro `"Error changing password for user with id {UserId}."`.
5. Sucesso → `output.AddResult(user.MapToDto())` (`UserResponse` não expõe `Password`, seguro devolver).

**Endpoint Api** (`PATCH /api/v1/users/{id}/password`, ver §6) implementado no mesmo dia — a decisão de método HTTP: `PUT` fica descartado porque a operação **não é idempotente** (uma segunda chamada idêntica falha, já que `CurrentPassword` deixa de bater depois da primeira troca), o que viola a exigência de idempotência do `PUT` por spec; `PATCH` não exige idempotência (RFC 5789) e descreve corretamente "atualização parcial de um campo do recurso `User`" — o argumento inicial contra `PATCH` (que ele só devia carregar o valor-alvo, não uma prova/precondição) foi revisto: nada impede um `PATCH` de carregar uma precondição (paralelo a `If-Match`/ETag), e como não há efeito colateral adicional hoje (sem invalidar sessão, sem e-mail de aviso, sem log de auditoria), `POST`-como-ação ficaria over-engineered pro que o sistema faz agora.

**Revogação de token discutida e descartada por enquanto**: trocar a senha **não invalida** nenhum JWT já emitido — eles continuam válidos até expirar (`Jwt:ExpirationInMinutes`, fallback 60min). O `JwtService` (ver §5) é totalmente stateless (sem `jti`, sem tabela de sessão, sem denylist), então revogação de verdade exigiria uma mudança estrutural na autenticação inteira (ex.: `TokenVersion`/`SecurityStamp` no `User` + validação customizada no pipeline JWT) — fora do escopo desta feature, registrado como limitação conhecida (ver §9).

Cobertura: unitários (`ChangePasswordUseCaseUnitTests` — 4 cenários: sucesso, usuário não existe, senha atual inválida, falha ao salvar) e integração (`ChangePasswordUseCaseIntegrationTests` — troca via `ICreateUserUseCase` real + confere com `IPasswordService.Verify` real (BCrypt) que a senha nova bate e a antiga não bate mais; falha usuário não existe; falha senha atual incorreta). Domínio: `UserUnitTests.User_ShouldChangePassword_WhenChangePasswordIsCalled` confirma `Password` alterado, `UpdatedAt` avançado, zero eventos novos.

#### `UseCases/Users/DeleteUser/` (novo, branch atual `feature/9-user-delete`)

`DeleteUserUseCase` (interface `IDeleteUserUseCase`), input `DeleteUserInput` — `readonly struct` (`CorrelationId`, `UserId`), mesmo desenho do `GetCustomerInput` (ver acima):
1. Busca o usuário por id (`GetByIdAsync`) — não existe → `LogWarning`, mas **não** chama `AddErrorMessage`; retorna o `Output` como está (`IsValid` continua `true`, sem `Result`, sem mensagem).
2. Se existe, `_userRepository.Remove(user)` (método já existia em `IRepository<T>`/`UserRepository` desde a Infrastructure original — ver §5 — mas nunca tinha sido exercitado por um use case real até agora, só por `UserRepositoryTests`) + `UnitOfWork.CommitAsync`.
3. Falha no commit → `LogError` + `AddErrorMessage($"Error deleting user with id {input.UserId}.")` (único caminho que deixa `IsValid = false`).
4. Sucesso → retorna o `Output` sem `AddResult`/`AddMessage` (nada em `Messages`/`Result`).

**Decisão de design deliberada, diferente de todos os outros use cases "por id"**: `UpdateUser`/`ChangePassword`/`GetCustomer` tratam "não encontrado" como erro (`AddErrorMessage("Unable to find user")`, `IsValid = false`). `DeleteUser` trata como **sucesso silencioso** — o motivo é modelar semântica REST idempotente de verdade: uma segunda chamada `DELETE` pro mesmo `userId` (retry de rede, duplo clique) não deve virar erro só porque o recurso já foi removido na primeira tentativa. Essa decisão foi tomada junto com o formato de resposta HTTP (ver §6): sucesso e "não existia" são **o mesmo resultado observável** (`204 No Content`), só falha real de persistência distingue os dois casos (`400`). Se algum dia precisar diferenciar "deletei agora" de "já não existia" (ex.: para telemetria), o jeito é `output.AddMessage(...)` em cada ramo, não `AddErrorMessage`.

Cobertura: unitários (`DeleteUserUseCaseUnitTests` — 3 cenários: sucesso quando existe [`Remove` chamado uma vez], sucesso idempotente quando não existe [`Remove` nunca chamado, `LogWarning` verificado], falha quando o commit retorna `false`) e integração (`DeleteUserUseCaseIntegrationTests` — cria via `ICreateUserUseCase` real, deleta, confere que `IUserRepository.GetByIdAsync` volta `null`; e um segundo teste confirmando que deletar um id inexistente também retorna `IsValid = true`).

#### `UseCases/Customers/UpdateCustomer/` (novo, branch atual `feature/10-update-customer`)

`UpdateCustomerUseCase` (interface `IUpdateCustomerUseCase`), input `UpdateCustomerInput(CorrelationId, CustomerId, Name, Email, Phone)` — **sem `Document`**, decisão de negócio consciente (ver §3: CPF/CNPJ é imutável depois de cadastrado, igual ao `UpdateUser` excluir `Password`):
1. Busca o customer por id (`GetByIdAsync`) — não existe → `LogWarning` + erro `"Unable to find customer"` (mesma mensagem do `GetCustomerUseCase`).
2. **Só checa e-mail duplicado se o e-mail estiver mudando** (mesmo raciocínio de idempotência do `UpdateUser`, ver acima) via `ICustomerRepository.ExistsWithEmailAsync` (novo, ver Interfaces abaixo) — se já pertence a outro customer, `LogWarning` + erro `"Customer with email {Email} already exists."`.
3. **Mesma lógica para `Phone`**: só checa se mudou, via `ICustomerRepository.ExistsWithPhoneAsync` (novo) — erro `"Customer with phone {Phone} already exists."`. **Motivo de existir esse segundo par de checagem/erro que `UpdateUser` não tem**: a tabela `Customers` tem índice único tanto em `Email` quanto em `Phone` (`AppDbContext.OnModelCreating`, ver §5) — `Users` só tem índice único em `Email`. Sem essa checagem, um `Phone` duplicado estouraria como `DbUpdateException` (violação de índice único) em vez de um erro amigável de `Output`, o mesmo tipo de bug documentado no `CreateUser` original (ver §4, bug do `CorrelationId` como `Id`).
4. Aplica `customer.UpdateProfile(input.Name, input.Email, input.Phone)` (método de comportamento no `Customer`, ver §3 — não reconstrói o agregado) e persiste (`Update` + `UnitOfWork.CommitAsync`).
5. Falha no commit → `LogError` + erro `"Error updating customer with id {CustomerId}."`.
6. Sucesso → `output.AddResult(customer.MapToDto())`, reaproveitando `CreateCustomerMapper.MapToDto` (sem mapper próprio, mesmo padrão do `GetCustomerUseCase`).

Cobertura: unitários (`UpdateCustomerUseCaseUnitTests` — 6 cenários: sucesso sem mudar e-mail/telefone, sucesso mudando os dois quando livres, falha customer não existe, falha e-mail já em uso, falha telefone já em uso, falha ao salvar) e integração (`UpdateCustomerUseCaseIntegrationTests` — persiste e relê via `ICustomerRepository.GetByIdAsync`, falha customer não existe, falha e-mail duplicado entre dois customers seedados, falha telefone duplicado entre dois customers seedados). Domínio: `CustomerUnitTests.Customer_ShouldUpdateProfile_WhenUpdateProfileIsCalled` confirma que `Name`/`Email`/`Phone` mudam, `Document` **não** muda, e `UpdatedAt` avança.

#### `UseCases/Vehicles/CreateVehicle/` (novo, branch atual `feature/11-create-vehicle`) — primeiro use case de um agregado além de `User`/`Customer`

`CreateVehicleUseCase` (interface `ICreateVehicleUseCase`), input `CreateVehicleInput(CorrelationId, CustomerId, LicensePlate, Brand, Model, ManufactureYear, ModelYear, Color)` — o `init` de `LicensePlate` normaliza o valor via `StringExtensions.NormalizeLicensePlate()` (uppercase + remove hífen), mesmo padrão do `Document` em `CreateCustomerInput`:
1. Checa se o `CustomerId` existe via `ICustomerRepository.GetByIdAsync` — **primeiro use case a validar uma referência cruzada entre agregados diferentes no Application** (`Vehicle` referencia `Customer` por Id); se não existir, `LogWarning` + erro `"Unable to find customer"`.
2. Checa placa duplicada via `IVehicleRepository.ExistsWithLicensePlateAsync` (novo) — se já existe, `LogWarning` + erro `"Vehicle with license plate {LicensePlate} already exists."`.
3. Monta o `Vehicle` via `input.MapToDomain()` (gera novo `Guid`, mesmo padrão do `CreateCustomer`) e persiste (`AddAsync` + `UnitOfWork.CommitAsync`). **Pode lançar `DomainException`** aqui se `ModelYear < ManufactureYear` (ver §3) — não é capturado localmente no use case, propaga pro `DomainExceptionHandler` global (ver §6).
4. Falha no commit → `LogError` + erro `"Error saving vehicle with license plate {LicensePlate}."`.
5. Sucesso → `output.AddResult(vehicle.MapToDto())`.

**Validação de formato de placa (Mercosul `AAA0A00` + antigo `AAA0000`) mora na Api** (`CreateVehicleRequestValidator`, via `StringExtensions.IsValidLicensePlate()` — regex `^[A-Z]{3}[0-9]([A-Z][0-9]{2}|[0-9]{3})$`), mesmo racional do CPF/CNPJ. **A checagem `ModelYear >= ManufactureYear` existe duas vezes, de propósito**: no validator da Api (`GreaterThanOrEqualTo`, dá erro de campo específico via `WithValidation<T>()`) **e** no construtor do `Vehicle` (defesa em profundidade — qualquer código que construa um `Vehicle` fora do fluxo HTTP, ex. um teste ou uma ferramenta futura, ainda é protegido).

Cobertura: unitários (`CreateVehicleUseCaseUnitTests` — 4 cenários: sucesso, customer não existe, placa duplicada, falha ao salvar) e integração (`CreateVehicleUseCaseIntegrationTests` — persiste e relê, falha customer não existe, falha placa duplicada entre dois customers). `TestData.LicensePlate()` (novo, `IntegrationTests/Fixtures/`) gera placa aleatória no formato antigo pra evitar colisão entre testes (mesmo racional do `TestData.Document()`).

**Mapper (`UseCases/Vehicles/CreateVehicle/Mapper/CreateVehicleMapper.cs`)**
- `MapToDomain(this CreateVehicleInput input)`: `new Vehicle(Guid.NewGuid(), input.CustomerId, input.LicensePlate, ..., DateTime.Now, DateTime.Now)`.
- `MapToDto(this Vehicle vehicle)`: `new VehicleResponse(Id, CustomerId, LicensePlate, Brand, Model, ManufactureYear, ModelYear, Color)` — reaproveitado por `GetVehicleUseCase` também (ver abaixo), mesmo padrão do `CreateCustomerMapper.MapToDto` reaproveitado por `GetCustomerUseCase`.

#### `UseCases/Vehicles/GetVehicle/` (novo, branch atual `feature/12-get-vehicle`)

`GetVehicleUseCase` (interface `IGetVehicleUseCase`), input `GetVehicleInput` — `readonly struct` (`CorrelationId`, `VehicleId`), mesmo molde do `GetCustomerInput`/`DeleteUserInput`:
1. Busca por id via `IVehicleRepository.GetByIdAsync` — não encontra → `LogWarning` + erro `"Unable to find vehicle"`.
2. Sucesso → `output.AddResult(vehicle.MapToDto())`.

Sem mapper próprio — reaproveita `CreateVehicleMapper.MapToDto`. Cobertura: unitários (`GetVehicleUseCaseUnitTests`) e integração (`GetVehicleUseCaseIntegrationTests` — cria customer+vehicle de verdade via os use cases reais e busca de volta; caso "não existe" usa um `Guid.NewGuid()` nunca inserido).

#### `UseCases/Services/CreateService/` (novo, branch atual `feature/13-create-service`) — terceiro agregado de negócio a ganhar Application+Api, primeiro sem referência cruzada a outro agregado

`CreateServiceUseCase` (interface `ICreateServiceUseCase`), input `CreateServiceInput(CorrelationId, Code, Name, Description, BasePrice, EstimatedDuration)` — sem normalização no `init` (diferente de `LicensePlate`/`Document`; `Code` de serviço não tem formato canônico definido no enunciado, então só existe checagem de obrigatoriedade/tamanho na Api):
1. Checa código duplicado via `IServiceRepository.ExistsWithCodeAsync` (novo) — se já existe, `LogWarning` + erro `"Service with code {Code} already exists."`.
2. Monta o `Service` via `input.MapToDomain()` (gera novo `Guid`, **`IsActive = true` sempre**, não vem do input — decisão do backlog) e persiste (`AddAsync` + `UnitOfWork.CommitAsync`). **Pode lançar `DomainException`** se `BasePrice`/`EstimatedDuration` forem negativos (ver §3) — propaga pro `DomainExceptionHandler` global, mesmo racional do `CreateVehicle`.
3. Falha no commit → `LogError` + erro `"Error saving service with code {Code}."`.
4. Sucesso → `output.AddResult(service.MapToDto())`.

Diferente do `CreateVehicle` (que valida uma referência a `Customer`), `CreateService` não depende de nenhum outro agregado — só a checagem de unicidade de `Code` (mesmo molde de `ExistsWithLicensePlateAsync`/`ExistsWithEmailAsync`).

Cobertura: unitários (`CreateServiceUseCaseUnitTests` — 3 cenários: sucesso, código duplicado, falha ao salvar) e integração (`CreateServiceUseCaseIntegrationTests` — persiste e relê via `IServiceRepository.GetByIdAsync`, falha código duplicado). Domínio: `ServiceUnitTests` ganhou 2 cenários novos (`BasePrice`/`EstimatedDuration` negativos lançam `DomainException` com a mensagem de `ServiceErrors` certa), além do cenário de criação bem-sucedida já existente.

**Mapper (`UseCases/Services/CreateService/Mapper/CreateServiceMapper.cs`)**
- `MapToDomain(this CreateServiceInput input)`: `new Service(Guid.NewGuid(), input.Code, input.Name, input.Description, input.BasePrice, input.EstimatedDuration, true, DateTime.Now, DateTime.Now)`.
- `MapToDto(this Service service)`: `new ServiceResponse(Id, Code, Name, Description, BasePrice, EstimatedDuration, IsActive)` — diferente de `CustomerResponse`, expõe todos os campos (nenhum é sensível), mesmo racional do `VehicleResponse`.

**Nota (2026-08-15, edição do usuário)**: `CreateServiceInput` foi convertido de classe (com primary constructor + `init`) para `sealed record` posicional logo depois de implementado — sem normalização em nenhum campo (diferente de `CreateVehicleInput.LicensePlate`), então o record simples é suficiente; nenhuma outra mudança de comportamento.

#### `UseCases/Services/GetServices/` (novo, branch atual `feature/14-get-services`)

`GetServicesUseCase` (interface `IGetServicesUseCase`), input é só um `Guid correlationId` solto — **sem boundary/record próprio**, mesmo molde do `GetUsersUseCase`:
1. Busca todos via `IServiceRepository.GetAllAsync(...)` — sem filtro, sem paginação, devolve a tabela `Services` inteira.
2. Se a lista vier vazia, loga `LogWarning`.
3. `output.AddResult(services.MapToDto())` **sempre**, independente de a lista estar vazia ou não — `IGetServicesUseCase` **corrige de propósito** a inconsistência documentada em `GetUsersUseCase` (ver §9 item 8): lá, o caminho vazio devolve `Array.Empty<User>()` (entidade de domínio crua) em vez do DTO, fazendo o JSON de `GET /api/v1/users` mudar de forma dependendo do estado do banco. Aqui não existe esse `if`/`else` de tipo — `services.MapToDto()` já lida com lista vazia sem problema, então sempre devolve `IEnumerable<ServiceResponse>`, vazio ou não. Se um dia o `GetUsersUseCase` for corrigido, esse é o padrão a seguir.
4. `output.IsValid` sempre `true` — "lista vazia" é sucesso, só logado como warning, mesmo racional do `GetUsersUseCase`.

Sem cobertura de integração para o cenário "lista vazia" — mesma razão do `GetUsersUseCase` (ver §4 acima): `DatabaseFixture` é compartilhado por toda a collection `"Integration"` sem reset entre testes, e `ServiceRepositoryTests`/`CreateServiceUseCaseIntegrationTests` já inserem `Service`s na mesma base, então a tabela nunca está garantidamente vazia nesse nível. Coberto só no unit test (mockado). Cobertura: unitários (`GetServicesUseCaseUnitTests` — sucesso com serviços via `AutoFixture.CreateMany<Service>()`, e sucesso+log quando a lista vem vazia) e integração (`GetServicesUseCaseIntegrationTests` — cria um serviço via `ICreateServiceUseCase` e confere que ele aparece no resultado).

**Mapper (`UseCases/Services/GetServices/Mapper/GetServicesMapper.cs`)**
- `MapToDto(this IEnumerable<Service> services)`: `services.Select(x => x.MapToDto())` — reaproveita `Service.MapToDto()` de `CreateServiceMapper` por extensão, mesmo padrão do `GetUsersMapper`.

#### `UseCases/InventoryItems/CreateInventoryItem/` (novo, branch atual `feature/15-create-inventory-item`) — quarto agregado de negócio a ganhar Application+Api

`CreateInventoryItemUseCase` (interface `ICreateInventoryItemUseCase`), input `CreateInventoryItemInput(CorrelationId, Code, Name, Description, QuantityOnHand, MinimumStock, UnitPrice, UnitOfMeasure)` — **sem `ReservedQuantity`**, decisão de negócio explícita do backlog: todo item nasce sem nada reservado (`ReservedQuantity = 0`, fixo no mapper, não vem do input), mesmo racional do `IsActive = true` fixo em `CreateService`:
1. Checa código duplicado via `IInventoryItemRepository.ExistsWithCodeAsync` (novo) — se já existe, `LogWarning` + erro `"Inventory item with code {Code} already exists."`.
2. Monta o `InventoryItem` via `input.MapToDomain()` (gera novo `Guid`, `ReservedQuantity = 0`) e persiste (`AddAsync` + `UnitOfWork.CommitAsync`). **Pode lançar `DomainException`** se `QuantityOnHand`/`MinimumStock`/`UnitPrice` forem negativos (ver §3) — propaga pro `DomainExceptionHandler` global, mesmo racional do `CreateVehicle`/`CreateService`.
3. Falha no commit → `LogError` + erro `"Error saving inventory item with code {Code}."`.
4. Sucesso → `output.AddResult(inventoryItem.MapToDto())`.

Mesmo molde estrutural do `CreateService`: sem referência cruzada a outro agregado, só checagem de unicidade de `Code`. Cobertura: unitários (`CreateInventoryItemUseCaseUnitTests` — 3 cenários: sucesso, código duplicado, falha ao salvar) e integração (`CreateInventoryItemUseCaseIntegrationTests` — persiste e relê via `IInventoryItemRepository.GetByIdAsync`, confere `ReservedQuantity == 0`, falha código duplicado). Domínio: `InventoryItemUnitTests` ganhou 5 cenários novos de invariante (ver §3).

**Mapper (`UseCases/InventoryItems/CreateInventoryItem/Mapper/CreateInventoryItemMapper.cs`)**
- `MapToDomain(this CreateInventoryItemInput input)`: `new InventoryItem(Guid.NewGuid(), input.Code, input.Name, input.Description, input.QuantityOnHand, 0, input.MinimumStock, input.UnitPrice, input.UnitOfMeasure, true, DateTime.Now, DateTime.Now)`.
- `MapToDto(this InventoryItem inventoryItem)`: `new InventoryItemResponse(Id, Code, Name, Description, QuantityOnHand, ReservedQuantity, MinimumStock, UnitPrice, UnitOfMeasure, IsActive)` — expõe todos os campos, mesmo racional do `VehicleResponse`/`ServiceResponse`.

#### `UseCases/InventoryItems/GetInventoryItems/` (novo, branch atual `feature/16-get-inventory-item`)

`GetInventoryItemsUseCase` (interface `IGetInventoryItemsUseCase`), input é só um `Guid correlationId` solto — mesmo molde do `GetServicesUseCase`/`GetUsersUseCase`, sem boundary próprio:
1. Busca todos via `IInventoryItemRepository.GetAllAsync(...)` — sem filtro, sem paginação, devolve a tabela `InventoryItems` inteira.
2. Se a lista vier vazia, loga `LogWarning` e `output.AddResult(Array.Empty<InventoryItemResponse>())` — **já devolve o DTO vazio, não a entidade** (diferente do `GetUsersUseCase` original, que tinha a inconsistência de tipo documentada acima; `GetServicesUseCase` já tinha corrigido isso antes, `GetInventoryItemsUseCase` segue o mesmo padrão certo desde o início).
3. Se houver itens, `output.AddResult(inventoryItems.MapToDto())` (`IEnumerable<InventoryItemResponse>`, via `GetInventoryItemsMapper`).
4. Em ambos os casos `output.IsValid` fica `true` — não existe caminho de erro real, "estoque vazio" é sucesso, só logado como warning.

`IInventoryItemRepository.GetAllAsync` é **método novo** (a interface só tinha `ExistsWithCodeAsync` desde `CreateInventoryItem`) — implementado em `InventoryItemRepository` (Infrastructure) e no `DomainMappers.MapToDomain(this IEnumerable<InventoryItemModel>)` correspondente (que também não existia; `Service`/`User` já tinham o par singular/`IEnumerable` desde antes, `InventoryItem` só tinha o singular).

Cobertura: unitários (`GetInventoryItemsUseCaseUnitTests` — sucesso com itens, sucesso+log quando a lista vem vazia) e integração (`GetInventoryItemsUseCaseIntegrationTests` — cria um item via `ICreateInventoryItemUseCase` real e confere que ele aparece no resultado; sem cenário de integração "lista vazia", mesmo racional do `GetServicesUseCaseIntegrationTests`/`GetUsersUseCaseIntegrationTests` — `DatabaseFixture` é compartilhado sem reset entre testes).

**Mapper (`UseCases/InventoryItems/GetInventoryItems/Mapper/GetInventoryItemsMapper.cs`)**
- `MapToDto(this IEnumerable<InventoryItem> inventoryItems)`: `inventoryItems.Select(x => x.MapToDto())` — reaproveita `InventoryItem.MapToDto()` de `CreateInventoryItemMapper` por extensão, mesmo padrão do `GetServicesMapper`/`GetUsersMapper`.

**Endpoint Api**: `GET /api/v1/inventory-items` com `.RequireAuthorization()` **sem policy nomeada** — mesma decisão de `GET /services` (consulta de catálogo/estoque acessível pelos três papéis, sem motivo de negócio pra restringir por role; ver §6).

#### `UseCases/ServiceOrders/CreateServiceOrder/` (novo, branch atual `feature/17-create-service-order`) — quinto agregado de negócio a ganhar Application+Api, primeiro use case de `ServiceOrder`

`CreateServiceOrderUseCase` (interface `ICreateServiceOrderUseCase`), input `CreateServiceOrderInput(CorrelationId, CustomerId, VehicleId, CreatedBy, ProblemDescription, OdometerReading)` — **`CreatedBy` não vem do corpo da requisição**, é injetado pela Api a partir de `ICurrentUserService.UserId` (ver §6), o mesmo racional de excluir campos que não deveriam ser confiáveis vindos do cliente:
1. Checa se o `CustomerId` existe via `ICustomerRepository.GetByIdAsync` — não existe → `LogWarning` + erro `"Unable to find customer"` (mesma mensagem/racional do `CreateVehicle`).
2. Checa se o `VehicleId` existe via `IVehicleRepository.GetByIdAsync` — não existe → `LogWarning` + erro `"Unable to find vehicle"`.
3. **Checa se o veículo pertence ao cliente informado** (`vehicle.CustomerId == input.CustomerId`) — segunda validação cruzada entre três agregados diferentes no mesmo use case (`ServiceOrder` referencia `Customer` **e** `Vehicle`, que por sua vez já referencia `Customer`); se não bater, `LogWarning` + erro `"Vehicle does not belong to the specified customer"`. Evita abrir uma OS pra um veículo que pertence a outro cliente por erro de digitação/id trocado no cliente.
4. Monta o `ServiceOrder` via `input.MapToDomain()` (`Guid.NewGuid()` para o `Id`, `Status` default `Received`, `OpenedAt = DateTime.Now`, sem orçamento — `Discount`/`Subtotal`/`Total` ficam no default `0`) e persiste (`AddAsync` + `UnitOfWork.CommitAsync`).
5. Falha no commit → `LogError` + erro `"Error saving service order"`.
6. Sucesso → `output.AddResult(serviceOrder.MapToDto())`.

Cobertura: unitários (`CreateServiceOrderUseCaseUnitTests` — 5 cenários: sucesso, cliente não existe, veículo não existe, veículo não pertence ao cliente, falha ao salvar) e integração (`CreateServiceOrderUseCaseIntegrationTests` — persiste e relê via `IServiceOrderRepository.GetByIdAsync`, falha cliente não existe, falha veículo não existe, falha veículo de outro cliente; o `CreatedBy` de teste é um `User` seedado direto via `IUserRepository`, já que a coluna `ServiceOrders.CreatedBy` tem FK pra `Users` — ver §5).

**Mapper (`UseCases/ServiceOrders/CreateServiceOrder/Mapper/CreateServiceOrderMapper.cs`)** — reaproveitado por `GetServiceOrder` também (ver abaixo), mesmo padrão do `CreateCustomerMapper`/`CreateVehicleMapper` reaproveitados por seus respectivos `GetById`
- `MapToDomain(this CreateServiceOrderInput input)`: `new ServiceOrder(Guid.NewGuid(), input.CustomerId, input.VehicleId, input.CreatedBy, input.ProblemDescription, input.OdometerReading, DateTime.Now, DateTime.Now, DateTime.Now)` — `openedAt` também `DateTime.Now`, `Status` fica no default `Received` do construtor.
- `MapToDto(this ServiceOrder serviceOrder)`: `new ServiceOrderResponse(Id, CustomerId, VehicleId, CreatedBy, Status.ToString(), ProblemDescription, DiagnoseDescription, OdometerReading, Discount, Subtotal, Total, OpenedAt, ClosedAt, Parts, Services, StatusHistory)` — `Status` exposto como `string` (`.ToString()`), mesmo racional do `UserResponse.Role` (não há `JsonStringEnumConverter` configurado globalmente, então um enum cru serializaria como número — ver `InventoryItemResponse.UnitOfMeasure`, que não faz essa conversão; inconsistência pré-existente, não corrigida aqui). **Correção em relação à nota anterior deste documento**: `Parts`/`Services`/`StatusHistory` acabaram **não** virando um DTO separado "mais rico" — reavaliado em `feature/18-get-service-order` (ver `CLAUDE.md` sobre postura crítica): todo outro agregado do projeto usa **um único** Response DTO reaproveitado entre `Create`/`Get`/`Update` (`CustomerResponse`, `VehicleResponse`, etc.), então criar um segundo tipo só pra `ServiceOrder` quebraria esse padrão sem necessidade real — o `ServiceOrderResponse` ganhou os três campos de coleção direto, sempre vazios na criação (`ServiceOrder.Parts`/`Services`/`StatusHistory` começam vazios) e populados quando `GetServiceOrder` os lê de volta. Mapeamento dos filhos também vive neste arquivo: `MapToDto(this ServiceOrderPart part)` → `ServiceOrderPartResponse(Id, InventoryItemId, Name, Description, UnitPrice, Quantity)`; `MapToDto(this ServiceOrderService service)` → `ServiceOrderServiceResponse(Id, ServiceId, Name, Description, UnitPrice, Quantity, EstimatedDuration)`; `MapToDto(this ServiceOrderStatusHistory statusHistory)` → `ServiceOrderStatusHistoryResponse(Id, PreviousStatus.ToString(), CurrentStatus.ToString(), ChangedBy, ChangedAt)`.

#### `UseCases/ServiceOrders/GetServiceOrder/` (novo, branch atual `feature/18-get-service-order`)

`GetServiceOrderUseCase` (interface `IGetServiceOrderUseCase`), input `GetServiceOrderInput` — `readonly struct` (`CorrelationId`, `ServiceOrderId`), mesmo molde do `GetCustomerInput`/`GetVehicleInput`:
1. Busca por id via `IServiceOrderRepository.GetByIdAsync` — já vem com `Parts`/`Services`/`StatusHistory` carregados (`.Include(...)` em `ServiceOrderRepository.GetByIdAsync`, ver §5, não precisou de nenhuma mudança pra isso). Não encontra → `LogWarning` + erro `"Unable to find service order"`.
2. Sucesso → `output.AddResult(serviceOrder.MapToDto())`.

Sem mapper próprio — reaproveita `CreateServiceOrderMapper.MapToDto` (ver acima), incluindo o mapeamento dos filhos. Cobertura: unitários (`GetServiceOrderUseCaseUnitTests` — sucesso, com asserção de que `Parts`/`Services`/`StatusHistory` vêm vazios pra uma OS recém-mockada; e não encontrado) e integração (`GetServiceOrderUseCaseIntegrationTests` — três cenários: sucesso sem filhos via `ICreateServiceOrderUseCase` real, sucesso **com** filhos, e não encontrado).

**Achado durante o teste de integração "com filhos", registrado mas não corrigido nesta branch** (fora de escopo pra `GetServiceOrder`, que só lê): tentar popular o cenário via "abrir OS pelo use case → carregar de volta → chamar `AddParts`/`AddServices`/`AddStatusHistory` no agregado já existente → `IServiceOrderRepository.Update(...)`" **não persiste os filhos novos**, silenciosamente. Causa raiz: `Repository<T>.Upsert` (ver §5), quando o agregado raiz não está rastreado pelo `ChangeTracker` (caso comum — `DbContext` novo por escopo), cai em `set.Update(model)`; esse `Update()` do EF Core percorre o grafo desconectado e marca cada entidade filha como `Added` só se a chave for o valor default — como `ServiceOrderPartModel`/`ServiceOrderServiceModel`/`ServiceOrderStatusHistoryModel` sempre chegam com um `Guid` já atribuído (construtor exige `id`, nunca um valor default), o EF marca todas como `Modified`, gera `UPDATE ... WHERE Id = @p` pra linhas que não existem, o SQL Server afeta zero linhas sem lançar exceção (nenhum concurrency token configurado), e `CommitAsync` devolve `true` porque a própria raiz teve alguma coluna escalar alterada. **Isso é exatamente o fluxo que o ticket 20 (`AddBudget`) vai precisar** ("carregar OS existente → adicionar peças/serviços → persistir") — `Repository<T>.Upsert`/`ServiceOrderRepository.Update` precisam de um ajuste (reconciliar a coleção filha por filha — marcar `Added` os novos, `Modified` os existentes, `Deleted` os removidos — em vez de um `set.Update(model)` cego num grafo desconectado) antes de `AddBudget` poder ser implementado corretamente. O teste de integração deste ticket contorna o problema montando a `ServiceOrder` inteira (raiz + filhos) **antes** do primeiro `AddAsync` (mesmo padrão do `ServiceOrderRepositoryTests.ShouldPersistChildren`, que nunca exercitou o caminho de update).

**Atualização (`feature/19-start-diagnosis`): esse gap foi parcialmente corrigido — ver `IServiceOrderRepository.UpdateAsync` em §5.** `StartDiagnosis` (abaixo) precisou exatamente desse fluxo (carregar OS → adicionar item novo a `StatusHistory` → persistir) uma etapa antes do previsto pelo backlog original. `Parts`/`Services` continuam com o gap descrito acima, pendentes pro ticket 20.

#### `UseCases/ServiceOrders/StartDiagnosis/` (novo, branch atual `feature/19-start-diagnosis`, ticket 19)

`StartDiagnosisUseCase` (interface `IStartDiagnosisUseCase`), input `StartDiagnosisInput(CorrelationId, ServiceOrderId, ChangedBy, DiagnoseDescription)`:
1. Busca a OS por id (`GetByIdAsync`) — não encontra → `LogWarning` + erro `"Unable to find service order"`.
2. Chama `serviceOrder.StartDiagnosis(input.DiagnoseDescription, input.ChangedBy)` (ver §3) — **sem checagem prévia de `Status` no use case**: a validação da transição mora inteira no agregado (`DomainException`), mesmo racional do `Vehicle`/`Service`/`InventoryItem` (invariante no construtor) — aqui é invariante num método de comportamento em vez do construtor, primeira vez que isso acontece no projeto. Se `Status != Received`, a exceção propaga sem `try/catch` local, até o `DomainExceptionHandler` global (ver §6) virar `400`.
3. Persiste via `IServiceOrderRepository.UpdateAsync` (método novo, não faz parte de `IRepository<T>` — só `ServiceOrder` tem, ver §5) + `UnitOfWork.CommitAsync`.
4. Falha no commit → `LogError` + erro `"Error updating service order with id {ServiceOrderId}."`.
5. Sucesso → `output.AddResult(serviceOrder.MapToDto())` — sem mapper próprio, reaproveita `CreateServiceOrderMapper.MapToDto`, mesmo padrão do `UpdateUser`/`GetServiceOrder`.

Cobertura: unitários (domínio: sucesso populando `StatusHistory` + `DomainException` numa segunda chamada; use case: sucesso, não encontrado, falha ao salvar) e integração (use case fim-a-fim provando persistência real do novo item de `StatusHistory`; mais um teste dedicado em `ServiceOrderRepositoryTests` reproduzindo exatamente o fluxo "carregar (`AsNoTracking`) → mutar em memória → `UpdateAsync`" pra provar que o fix da §5 funciona, não só o caminho feliz do `CreateServiceOrder`).

#### `UseCases/ServiceOrders/AddBudget/` (novo, branch atual `feature/20-add-budget`, ticket 20)

`AddBudgetUseCase` (interface `IAddBudgetUseCase`), input `AddBudgetInput(CorrelationId, ServiceOrderId, ChangedBy, Services: IReadOnlyCollection<AddBudgetServiceItem>, Parts: IReadOnlyCollection<AddBudgetPartItem>)` — **sem `Discount`** (ver §3, decisão revista) — `AddBudgetServiceItem(ServiceId, Quantity)`/`AddBudgetPartItem(InventoryItemId, Quantity)` são records auxiliares no mesmo arquivo do input (não boundary próprio cada um — cohesão, só usados aqui). **Primeiro use case do projeto a injetar três repositórios e orquestrar dois agregados na mesma transação** (`ServiceOrder` + `InventoryItem`, via `IServiceOrderRepository`/`IServiceRepository`/`IInventoryItemRepository`):
1. Busca a OS por id — não encontra → `LogWarning` + erro `"Unable to find service order"`.
2. Para cada peça pedida: busca o `InventoryItem` — não encontra → `LogWarning` + erro `"Unable to find inventory item"`, retorna. Se encontrar, já chama `inventoryItem.Reserve(partItem.Quantity)` (ver §3) e monta o `ServiceOrderPart` "fotografado" via `inventoryItem.MapToServiceOrderPart(serviceOrder.Id, partItem.Quantity)` (extension method no `Mapper/AddBudgetMapper.cs` da Application, ver abaixo — não é `new ServiceOrderPart(...)` inline no use case) — tudo no mesmo loop, sem um loop separado pareando duas listas por índice (jeito inicial, trocado por ser frágil).
3. Mesma coisa para cada serviço pedido, contra `IServiceRepository` — erro `"Unable to find service"` se não encontrar. Monta o `ServiceOrderService` fotografado via `service.MapToServiceOrderService(serviceOrder.Id, serviceItem.Quantity)` (mesmo mapper). **Sem `Reserve` equivalente** — `Service` não tem estoque, só `InventoryItem` tem.
4. Chama `serviceOrder.AddBudget(orderServices, parts, input.ChangedBy)` (ver §3) — pode lançar `DomainException` (`Status != Diagnosing`), sem `try/catch` local, propagando até o `DomainExceptionHandler` global. **`Reserve` (passo 2) roda antes desta checagem de status** — inofensivo, porque nada persiste até o `CommitAsync` final (passo 6); se a OS não estiver em `Diagnosing`, a reserva em memória é só descartada junto com o resto do request.
5. Se um serviço não existir (passo 3), a reserva já feita em memória nos itens de peças anteriores (passo 2) também é descartada sem nunca ter sido persistida — não há rollback explícito porque não há nada para desfazer no banco.
6. Persiste: `IServiceOrderRepository.UpdateAsync(serviceOrder, ct)` (reconcilia `Parts`/`Services`/`StatusHistory`, ver §5) + `IInventoryItemRepository.Update(item)` (sync, sem filhos, sem gap) pra cada peça reservada — todos compartilham o mesmo `AppDbContext` do escopo (repositórios `Scoped`), então um único `UnitOfWork.CommitAsync` (via `_serviceOrderRepository`) persiste tudo atomicamente.
7. Falha no commit → `LogError` + erro `"Error updating service order with id {ServiceOrderId}."` (mesma mensagem do `StartDiagnosis`).
8. Sucesso → `output.AddResult(serviceOrder.MapToDto())` — sem mapper próprio de DTO, reaproveita `CreateServiceOrderMapper.MapToDto` (já mapeia `Parts`/`Services`/`StatusHistory`, ver §4 `GetServiceOrder`).

**Por que `Reserve` não migrou pro método `ServiceOrder.AddBudget`** (pergunta levantada pelo usuário durante a review): `ServiceOrder` e `InventoryItem` são agregados diferentes, sem referência viva entre si (`ServiceOrderPart` guarda só `InventoryItemId`). `ServiceOrder.AddBudget` mutar instâncias de `InventoryItem` diretamente quebraria a fronteira entre agregados — a regra "um agregado por transação de negócio, referenciados só por Id" já seguida em todo o resto do projeto. `Reserve()` já mora corretamente em `InventoryItem` (sua própria invariante); o loop que o chama é orquestração pura, sem regra de negócio embutida, então pertence à Application (`AddBudgetUseCase`) — mesma decisão já fechada no backlog antes desta branch existir (ver `docs/backlog-proximas-tarefas.md`, "Decisões de design já fechadas" #3).

**Mapper (`UseCases/AddBudget/Mapper/AddBudgetMapper.cs`, novo)** — só instanciação, sem lógica: `MapToServiceOrderPart(this InventoryItem inventoryItem, Guid serviceOrderId, int quantity)` e `MapToServiceOrderService(this Service service, Guid serviceOrderId, int quantity)`, cada um só um `new ServiceOrderPart(...)`/`new ServiceOrderService(...)` "fotografando" os campos do catálogo. Extraído do corpo do use case a pedido do usuário — o `Handle` tinha ~30 linhas de instanciação inline que ficaram só duas chamadas de extension method.

Cobertura: unitários (domínio: `InventoryItem.Reserve` sucesso/estoque insuficiente, `ServiceOrder.AddBudget` sucesso/status inválido; use case: sucesso, OS não encontrada, peça não encontrada, serviço não encontrado, falha ao salvar) e integração (use case fim-a-fim contra SQL Server real: sucesso reservando estoque de verdade, peça não encontrada, `DomainException` por estoque insuficiente, `DomainException` por OS fora de `Diagnosing`; mais um teste em `ServiceOrderRepositoryTests` provando que `UpdateAsync` reconcilia `Parts`/`Services` — não só `StatusHistory` — corretamente).

#### `UseCases/ServiceOrders/ApproveServiceOrder/` (novo, branch atual `feature/21-approve-service-order`, ticket 21)

`ApproveServiceOrderUseCase` (interface `IApproveServiceOrderUseCase`), input `ApproveServiceOrderInput(CorrelationId, ServiceOrderId, ChangedBy)` — sem payload de negócio além do Id (é só um registro de decisão), mas continua `record` em vez de `readonly struct` porque carrega `ChangedBy` (o padrão `readonly struct` é reservado pros boundaries "só consulta por id", ver `GetCustomerInput`/`GetServiceOrderInput`), mesmo racional do `StartDiagnosisInput`/`ApproveServiceOrderInput` acima. Mais simples que o `AddBudgetUseCase` — só dois repositórios, um agregado orquestrado por vez:
1. Busca a OS por id — não encontra → `LogWarning` + erro `"Unable to find service order"`.
2. Para cada `ServiceOrderPart` **já persistido na OS** (veio do orçamento aprovado no ticket 20, não de input novo) — busca o `InventoryItem` correspondente por `part.InventoryItemId`; não encontra → `LogWarning` + erro `"Unable to find inventory item"` (mesma mensagem do `AddBudget`), retorna. Se encontrar, chama `inventoryItem.CommitReservation(part.Quantity)` (ver §3) — pode lançar `DomainException`, mesmo racional do `Reserve` no `AddBudget` (sem checagem prévia no use case, `ServiceOrder.Approve` ainda nem foi chamado nesse ponto).
3. Chama `serviceOrder.Approve(input.ChangedBy)` (ver §3) — pode lançar `DomainException` (`Status != AwaitingApproval`).
4. Persiste: `IServiceOrderRepository.UpdateAsync` + `IInventoryItemRepository.Update` por item debitado, um único `CommitAsync`.
5. Falha no commit → `LogError` + erro `"Error updating service order with id {ServiceOrderId}."` (mesma mensagem do `StartDiagnosis`/`AddBudget`).
6. Sucesso → `output.AddResult(serviceOrder.MapToDto())` — sem mapper próprio, reaproveita `CreateServiceOrderMapper.MapToDto`.

Cobertura: unitários (domínio: `InventoryItem.CommitReservation` sucesso/excede reservado, `ServiceOrder.Approve` sucesso/status inválido; use case: sucesso, OS não encontrada, `InventoryItem` não encontrado, falha ao salvar) e integração (use case fim-a-fim contra SQL Server real: sucesso debitando estoque de verdade — `QuantityOnHand`/`ReservedQuantity` conferidos direto no banco —, OS não encontrada, `DomainException` por OS fora de `AwaitingApproval`).

**Bug real encontrado nos testes de integração deste ticket (não é do `ApproveServiceOrder`, é do `IServiceOrderRepository.UpdateAsync` do ticket 19/20)**: os testes de integração deste projeto costumam encadear vários use cases dentro do mesmo `using var scope` pra montar o cenário (ex.: `CreateServiceOrder` → `StartDiagnosis` → `AddBudget`, todos compartilhando o mesmo `AppDbContext` do escopo). Isso expôs que `UpdateAsync` (ver §5) não verificava se uma entidade filha (`ServiceOrderPartModel`/`ServiceOrderServiceModel`/`ServiceOrderStatusHistoryModel`) já estava rastreada pelo `ChangeTracker` de uma chamada *anterior* no mesmo `DbContext` antes de tentar anexar uma instância nova desconectada com o mesmo Id — o EF Core lança `InvalidOperationException` ("cannot be tracked because another instance with the same key value is already being tracked"). A raiz (`ServiceOrderModel`) já tinha essa checagem desde o início; os filhos não. Corrigido — ver §5.

#### `UseCases/ServiceOrders/RejectServiceOrder/` (novo, branch atual `feature/22-reject-service-order`, ticket 22)

`RejectServiceOrderUseCase` (interface `IRejectServiceOrderUseCase`), input `RejectServiceOrderInput(CorrelationId, ServiceOrderId, ChangedBy)` — **estrutura idêntica ao `ApproveServiceOrderInput`**, mesmo racional (registro de decisão do atendente, sem payload de negócio além do Id). O `Handle` inteiro é uma cópia quase literal do `ApproveServiceOrderUseCase`, só trocando dois pontos:
1. Busca a OS por id — não encontra → `LogWarning` + erro `"Unable to find service order"` (mesma mensagem do `ApproveServiceOrder`).
2. Para cada `ServiceOrderPart` já persistido na OS — busca o `InventoryItem` correspondente; não encontra → `LogWarning` + erro `"Unable to find inventory item"` (mesma mensagem). Se encontrar, chama `inventoryItem.ReleaseReservation(part.Quantity)` (ver §3) **em vez de `CommitReservation`** — pode lançar `DomainException`, mesmo racional do `Approve`.
3. Chama `serviceOrder.Reject(input.ChangedBy)` (ver §3) **em vez de `Approve`** — pode lançar `DomainException` (`Status != AwaitingApproval`).
4. Persiste: `IServiceOrderRepository.UpdateAsync` + `IInventoryItemRepository.Update` por item liberado, um único `CommitAsync`.
5. Falha no commit → `LogError` + erro `"Error updating service order with id {ServiceOrderId}."` (mesma mensagem do `StartDiagnosis`/`AddBudget`/`Approve`).
6. Sucesso → `output.AddResult(serviceOrder.MapToDto())` — sem mapper próprio, reaproveita `CreateServiceOrderMapper.MapToDto`.

Cobertura: unitários (domínio: `InventoryItem.ReleaseReservation` sucesso/excede reservado, `ServiceOrder.Reject` sucesso/status inválido; use case: sucesso, OS não encontrada, `InventoryItem` não encontrado, falha ao salvar — mesmos quatro cenários do `ApproveServiceOrderUseCaseUnitTests`) e integração (use case fim-a-fim contra SQL Server real: sucesso liberando a reserva sem debitar `QuantityOnHand` — conferido direto no banco —, OS não encontrada, `DomainException` por OS fora de `AwaitingApproval`).

### Interfaces / portas (`Interfaces/`)
- `Repositories/IUnitOfWork.CommitAsync()`
- `Repositories/IUserRepository : IRepository<User>` — `GetByEmailAsync(string email, ...)`, `ExistsWithEmailAsync(string email, ...)`, `GetAllAsync(...)` (novo, `feature/7-get-users` — sem paginação/filtro, devolve `IEnumerable<User>` com a tabela inteira)
- `Repositories/ICustomerRepository : IRepository<Customer>` — `AnyAsync(string document, ...)` (adicionado com o `CreateCustomer` use case; antes a interface era vazia, seguindo a política incremental de repositório — ver §9), mais `ExistsWithEmailAsync(string email, ...)`/`ExistsWithPhoneAsync(string phone, ...)` (novos, `feature/10-update-customer`, adicionados só quando `UpdateCustomer` precisou checar duplicidade de e-mail/telefone — mesmo molde do `IUserRepository.ExistsWithEmailAsync`)
- `Repositories/IVehicleRepository : IRepository<Vehicle>` — `ExistsWithLicensePlateAsync(string licensePlate, ...)` (novo, `feature/11-create-vehicle`, mesmo molde de `ICustomerRepository.AnyAsync`/`IUserRepository.ExistsWithEmailAsync`)
- `Repositories/IServiceRepository : IRepository<Service>` — `ExistsWithCodeAsync(string code, ...)` (novo, `feature/13-create-service`, mesmo molde de `ExistsWithLicensePlateAsync`); `GetAllAsync(...)` (novo, `feature/14-get-services`, mesmo molde de `IUserRepository.GetAllAsync` — sem paginação/filtro)
- `Repositories/IInventoryItemRepository : IRepository<InventoryItem>` — `ExistsWithCodeAsync(string code, ...)` (`feature/15-create-inventory-item`, mesmo molde de `IServiceRepository.ExistsWithCodeAsync`); `GetAllAsync(...)` (novo, `feature/16-get-inventory-item`, mesmo molde de `IServiceRepository.GetAllAsync`)
- `Abstractions/IRepository<T> where T : AggregateRoot` — **sem segundo parâmetro `TId`** (assume `Guid` implicitamente): `UnitOfWork`, `GetByIdAsync`, `AddAsync`, `Update`, `Remove`. `Repositories/IServiceOrderRepository : IRepository<ServiceOrder>` — continua **vazia** (sem método extra) mesmo depois do `CreateServiceOrder`/`GetServiceOrder` consumirem `AddAsync`/`GetByIdAsync` (herdados de `IRepository<T>`, nenhum método próprio precisou ser adicionado ainda).
- `Services/ICurrentUserService` — `UserId` (`Guid`), `Email` (`string`), `Role` (`string`), `IsAuthenticated` (lido do `ClaimsPrincipal`)
- `Services/IDomainEventDispatcher.DispatchAsync(events, cancellationToken)`
- `Services/IJwtService.GenerateToken(User user)` — **mudou desde a última reescrita**: antes recebia três `string` soltos (`userId`, `email`, `role`); hoje recebe a entidade `User` inteira e extrai os claims internamente (ver implementação em §5).
- `Services/IPasswordService` — `Hash(string rawPassword)`, `Verify(string rawPassword, string hash)`. Mora em `Application.Interfaces.Services` (não em Domain) — ver nota em §10 sobre essa diferença em relação a uma iteração anterior do projeto.
- `Abstractions/IDomainEventHandler<TEvent>` — infraestrutura de handlers de evento de domínio (nenhum handler concreto implementado ainda — ver §11)
- `UseCases/ICreateUserUseCase`, `ILoginUserUseCase`, `ICreateCustomerUseCase`, `IGetCustomerUseCase`, `IGetUsersUseCase`, `IUpdateUserUseCase`, `IChangePasswordUseCase`, `IDeleteUserUseCase`, `IUpdateCustomerUseCase`, `ICreateVehicleUseCase`, `IGetVehicleUseCase`, `ICreateServiceUseCase`, `IGetServicesUseCase`, `ICreateInventoryItemUseCase`, `IGetInventoryItemsUseCase`, `ICreateServiceOrderUseCase`, `IGetServiceOrderUseCase`, `IStartDiagnosisUseCase`, `IAddBudgetUseCase`, `IApproveServiceOrderUseCase`, `IRejectServiceOrderUseCase` — um por use case, todos só com `Handle(TInput, CancellationToken) : Task<Output>` (em `IGetUsersUseCase`, `TInput` é só `Guid correlationId`, sem boundary próprio — ver acima). Continuam soltos em `Interfaces/UseCases/` sem subpasta por agregado — só a implementação concreta em `UseCases/` foi reorganizada (ver nota no topo desta seção).

### Commons (`Commons/`)
- `Output.cs` — ver acima.
- `StringExtensions.cs`: `StandardizeDocument(this string document)` (formata CPF/CNPJ sem pontuação via `CpfCnpjLibrary`, baseado no tamanho — 11 = CPF, 14 = CNPJ, outro tamanho lança `ArgumentException`) e `IsValidDocument(this string document)` (valida dígito verificador; tamanho fora de 11/14 retorna `false` em vez de lançar). Usado por `CreateCustomerInput` (normalização) e por `CreateCustomerRequestValidator` na Api (validação de fato, ver §6). **Novos em `feature/11-create-vehicle`**: `NormalizeLicensePlate(this string licensePlate)` (uppercase + remove hífen) e `IsValidLicensePlate(this string licensePlate)` (regex Mercosul/antigo via `[GeneratedRegex]` — source generator, não `Regex` em runtime; é por isso que a classe virou `public static partial class StringExtensions`, com o método do regex também `partial`).

### DTOs
- `DTOs/Users/UserResponse(Id, Name, Email, Role)` — usado por `CreateUserUseCase.MapToDto`, por `GetUsersUseCase` (via `GetUsersMapper`), e agora também por `UpdateUserUseCase`/`ChangePasswordUseCase` (chamando `user.MapToDto()` direto, sem mapper próprio — ver acima); `Role` é `string` (`user.Role.ToString()`). Não expõe `Password`, então é seguro devolver mesmo no fluxo de troca de senha.
- `DTOs/Users/LoginResponse(Token)` — segue existindo como DTO, mas `LoginUserUseCase` devolve o token como `string` cru (`output.AddResult(token)`), não empacotado num `LoginResponse` — parece scaffold ainda não conectado.
- `DTOs/Customer/CustomerResponse(Id, Name, Phone)` — usado por `CreateCustomerUseCase.MapToDto` **e** por `GetCustomerUseCase`/`UpdateCustomerUseCase` (ambos reaproveitam o mesmo mapper, ver acima). Note que **não inclui `Document` nem `Email`** na resposta — inclusive no retorno do `UpdateCustomerUseCase`, então o teste de integração de `UpdateCustomer` precisa guardar o `Email`/`Phone` usados no `CreateCustomerInput` original pra conseguir comparar depois (o `CustomerResponse` não devolve esses campos pra verificar).
- `DTOs/Vehicle/VehicleResponse(Id, CustomerId, LicensePlate, Brand, Model, ManufactureYear, ModelYear, Color)` (novo, `feature/11-create-vehicle`) — usado por `CreateVehicleUseCase.MapToDto` **e** por `GetVehicleUseCase` (mesmo mapper reaproveitado). Diferente de `CustomerResponse`, expõe **todos** os campos, incluindo `CustomerId` — nenhum deles é sensível (não é como CPF/e-mail).
- `DTOs/Service/ServiceResponse(Id, Code, Name, Description, BasePrice, EstimatedDuration, IsActive)` (novo, `feature/13-create-service`) — usado por `CreateServiceUseCase.MapToDto`. Expõe todos os campos, mesmo racional do `VehicleResponse`.
- `DTOs/InventoryItem/InventoryItemResponse(Id, Code, Name, Description, QuantityOnHand, ReservedQuantity, MinimumStock, UnitPrice, UnitOfMeasure, IsActive)` (novo, `feature/15-create-inventory-item`) — usado por `CreateInventoryItemUseCase.MapToDto`. Expõe todos os campos, mesmo racional do `VehicleResponse`/`ServiceResponse`.
- `DTOs/ServiceOrder/ServiceOrderResponse(Id, CustomerId, VehicleId, CreatedBy, Status, ProblemDescription, DiagnoseDescription, OdometerReading, Discount, Subtotal, Total, OpenedAt, ClosedAt)` (novo, `feature/17-create-service-order`) — usado por `CreateServiceOrderUseCase.MapToDto`. **Sem `Number`** (removido do agregado, ver §3) e sem `Parts`/`Services`/`StatusHistory` (sempre vazios na criação); `Status` exposto como `string`, diferente do `UnitOfMeasure` cru do `InventoryItemResponse` — ver nota de inconsistência em §4.

### Composição (`IoC/DependencyInjection.cs`)
`AddApplication()` registra `ICreateUserUseCase`, `ILoginUserUseCase`, `ICreateCustomerUseCase`, `IGetCustomerUseCase`, `IGetUsersUseCase`, `IUpdateUserUseCase`, `IChangePasswordUseCase`, `IDeleteUserUseCase`, `IUpdateCustomerUseCase`, `ICreateVehicleUseCase`, `IGetVehicleUseCase`, `ICreateServiceUseCase`, `IGetServicesUseCase`, `ICreateInventoryItemUseCase`, `IGetInventoryItemsUseCase`, `ICreateServiceOrderUseCase`, `IGetServiceOrderUseCase`, `IStartDiagnosisUseCase`, `IAddBudgetUseCase`, `IApproveServiceOrderUseCase` e `IRejectServiceOrderUseCase`, todos como `Scoped`.

## 5. Camada de Infraestrutura (`Fiap.Workshop.Infrastructure`)

> ⚠️ Ajustada em 2026-08-08 junto com §3/§4 — nomes de tipo (`IPasswordHasher`→`IPasswordService`), comportamento do `CommitAsync` e o estado real dos repositórios dos demais agregados estavam desatualizados nesta seção.

### Persistência (EF Core, SQL Server)
- `AppDbContext` (implementa `IUnitOfWork`): `DbSet` para todos os agregados (`Users`, `Customers`, `Vehicles`, `InventoryItems`, `Services`, `ServiceOrders`); `CommitAsync` roda `SaveChangesAsync` num `try/catch` **próprio** (se falhar, loga `LogError` e retorna `false` direto); só depois, **num `try/catch` separado**, entrega os eventos pendentes a `IDomainEventDispatcher.DispatchAsync` — se o dispatch falhar, só loga (não derruba o resultado do commit). O bug descrito numa versão anterior deste documento (um único `try/catch` em volta dos dois, fazendo falha de dispatch parecer falha de persistência) **já está corrigido** — ver §11.
- `UserModel` (`Repositories/Models/`): `Id`, `Email`, `Name`, `Password` (**`string`**, não `varbinary`/`byte[]` — refatorado em 2026-08-07), `Role`, `CreatedAt` (`DateTime`, não nulo), `UpdatedAt` (`DateTime?`). Vêm do domínio via `MapToModel`/`MapToDomain` — sem default de banco (`GETUTCDATE()`); o `User` é a fonte de verdade para essas datas.
- Mapeamento tabela `Users`: `Email` único (`HasIndex().IsUnique()`), `MaxLength(256)`; `Name` `MaxLength(100)`; `Password` `MaxLength(60)` (tamanho fixo de um hash BCrypt); `Role` `HasConversion<string>().HasMaxLength(20)`.
- `UserRepository : IUserRepository` — usa `AsNoTracking()` para leituras; mapeia Model↔Domain via `DomainMappers.MapToDomain`/`ModelMappers.MapToModel`, que chamam `new User(...)`/`new UserModel(...)` diretamente (**não existe `User.Rehydrate`**, ver §3 sobre o efeito colateral disso no `UserCreatedEvent`); enfileira eventos de domínio no `AppDbContext` (`EnqueueEvents`) só em `AddAsync`/`Update`, nunca em leituras. `GetAllAsync` (novo, `feature/7-get-users`): `_context.Users.AsNoTracking().ToListAsync(...)` + `DomainMappers.MapToDomain(this IEnumerable<UserModel>)` (novo overload, `model.Select(MapToDomain)`) — sem filtro, sem paginação, mesma observação de "leitura não enfileira evento" se aplica aqui também. `Update` (herdado, já existia desde antes) até `feature/7-get-users` só era exercitado por testes (`UserRepositoryTests`); desde `feature/8-put-users` é usado de verdade por `UpdateUserUseCase`/`ChangePasswordUseCase` (ver §4) — é por isso que o bug do `UserCreatedEvent` em `Update` (§3) só virou um problema prático agora, nunca antes. Mesma história para `Remove` (`Delete(_context.Users, entity.MapToModel(), entity.Id)`, herdado de `Repository<T>`): existia desde a Infrastructure original só como suporte genérico e só era exercitado por `UserRepositoryTests`; `DeleteUserUseCase` (`feature/9-user-delete`, ver §4) é o primeiro use case a chamá-lo de verdade.
- `CustomerRepository : ICustomerRepository` — mesmo molde de `UserRepository` (`AsNoTracking()` em leituras), mais `AnyAsync(string document, ...)` (`_context.Customers.AsNoTracking().AnyAsync(x => x.Document == document, ...)`), adicionado junto com o `CreateCustomer` use case (2026-08-11) — **primeiro repositório de negócio (fora `User`) a ganhar um método além do `IRepository<T>` base**, confirmando a política incremental (ver §9). `ExistsWithEmailAsync`/`ExistsWithPhoneAsync` (novos, `feature/10-update-customer`, mesmo molde de `AnyAsync`) adicionados quando `UpdateCustomerUseCase` precisou checar duplicidade de e-mail/telefone (ver §4). `Update` (herdado, existia desde a Infrastructure original) até esta branch só era exercitado por `CustomerRepositoryTests` — `UpdateCustomerUseCase` é o primeiro use case a chamá-lo de verdade, mesma história de `Update`/`Remove` em `UserRepository` (acima).
- `VehicleRepository : IVehicleRepository` — mesmo molde de `UserRepository`/`CustomerRepository`, mais `ExistsWithLicensePlateAsync(string licensePlate, ...)` (novo, `feature/11-create-vehicle`, `_context.Vehicles.AsNoTracking().AnyAsync(x => x.LicensePlate == licensePlate, ...)`).
- `ServiceRepository : IServiceRepository` — mesmo molde de `UserRepository`/`CustomerRepository`/`VehicleRepository`, mais `ExistsWithCodeAsync(string code, ...)` (novo, `feature/13-create-service`, `_context.Services.AsNoTracking().AnyAsync(x => x.Code == code, ...)`) e `GetAllAsync(...)` (novo, `feature/14-get-services`, mesmo molde do `UserRepository.GetAllAsync` — `_context.Services.AsNoTracking().ToListAsync(...)` + `DomainMappers.MapToDomain(this IEnumerable<ServiceModel>)`, novo overload adicionado junto).
- `InventoryItemRepository : IInventoryItemRepository` — mesmo molde de `ServiceRepository`, mais `ExistsWithCodeAsync(string code, ...)` (novo, `feature/15-create-inventory-item`, `_context.InventoryItems.AsNoTracking().AnyAsync(x => x.Code == code, ...)`).
- `ServiceOrderRepository : IServiceOrderRepository` — mesmo molde de `UserRepository` para `GetByIdAsync`/`AddAsync`/`Update`(sync)/`Remove` (herdados/`Upsert` de `Repository<T>`). `GetByIdAsync` inclui os três filhos via `.Include(so => so.Parts).Include(so => so.Services).Include(so => so.StatusHistory)` — consumido primeiro por `CreateServiceOrder`/`GetServiceOrder` (`feature/17`/`feature/18`) de verdade, antes só exercitado por `ServiceOrderRepositoryTests`. **`Update` (sync) tem um gap real, descoberto em `feature/18-get-service-order`**: `Repository<T>.Upsert` (abaixo), quando a raiz não está rastreada pelo `ChangeTracker` (comum — `DbContext` novo por escopo), cai em `set.Update(model)`. O `Update()` do EF Core percorre o grafo desconectado e só marca uma entidade como `Added` se a chave for o valor default; como `ServiceOrderPartModel`/`ServiceOrderServiceModel`/`ServiceOrderStatusHistoryModel` sempre chegam com um `Guid` explícito (construtor exige `id`, nunca default), o EF marca todas como `Modified` — gera `UPDATE ... WHERE Id = @p` pra linhas que não existem, o SQL Server afeta zero linhas sem lançar exceção (sem concurrency token configurado), e `CommitAsync` devolve `true` porque a raiz teve alguma coluna escalar alterada. Resultado: filhos novos adicionados a uma OS já persistida (`serviceOrder.AddParts(...)` + `Update()`) são **silenciosamente descartados**.
  - **`UpdateAsync` (método novo, `feature/19-start-diagnosis`, estendido em `feature/20-add-budget`) — corrige isso pras três coleções agora**: adicionado direto em `IServiceOrderRepository` (não em `IRepository<T>` — nenhum outro agregado tem esse problema, já que só `ServiceOrder` tem coleções-filhas), assinatura `Task UpdateAsync(ServiceOrder entity, CancellationToken cancellationToken)`. Implementação: atualiza a raiz via `_context.Entry(model).State = EntityState.Modified` (ou `CurrentValues.SetValues` se já rastreada) — **sem** usar `set.Update(model)`, que é o que cascateava o estado errado pros filhos — depois, pra cada uma das três coleções (`Parts`/`Services`/`StatusHistory`), consulta os ids já existentes no banco pra este `ServiceOrderId` (`_context.Set<TModel>().Where(x => x.ServiceOrderId == entity.Id).Select(x => x.Id).ToListAsync(...)`) e chama `ReconcileChildren<TModel>` pra cada item. `StartDiagnosisUseCase` (só `StatusHistory`) e `AddBudgetUseCase`/`ApproveServiceOrderUseCase` (`Parts`/`Services`/`StatusHistory`, ver §4) são os consumidores. **Gap do ticket 18 totalmente fechado.**
  - **`ReconcileChildren<TModel>(List<TModel> incoming, Func<TModel, Guid> idSelector, IReadOnlyCollection<Guid> existingIds)` (método privado, novo em `feature/21-approve-service-order`)**: extraído dos três blocos quase-idênticos que existiam inline (a duplicação virou lógica real, não só estrutura, então passou a justificar a abstração — diferente de quando só tinha `StatusHistory`, ver nota no ticket 19). **Bug corrigido aqui**: os três blocos originais nunca checavam se uma entidade filha já estava rastreada pelo `ChangeTracker` de uma chamada *anterior* no mesmo `DbContext`/escopo antes de tentar `_context.Entry(item).State = ...` — só a raiz (`ServiceOrderModel`) tinha essa checagem desde o início. Descoberto nos testes de integração deste ticket, que encadeiam `StartDiagnosis` → `AddBudget` no mesmo `using var scope` pra montar o cenário de `Approve`: a segunda chamada a `UpdateAsync` (do `AddBudget`) tentava anexar uma `ServiceOrderStatusHistoryModel` nova e desconectada com o mesmo Id de uma que a primeira chamada (do `StartDiagnosis`) já tinha deixado rastreada após o `SaveChanges` — `InvalidOperationException` ("cannot be tracked because another instance with the same key value is already being tracked"). `ReconcileChildren` agora procura no `ChangeTracker` primeiro (`_context.ChangeTracker.Entries<TModel>().FirstOrDefault(e => idSelector(e.Entity) == id)`) e usa `CurrentValues.SetValues` se achar, mesmo padrão já usado pela raiz. Coberto por um teste de regressão dedicado em `ServiceOrderRepositoryTests` que chama `UpdateAsync` duas vezes seguidas no mesmo escopo.
- **(2026-08-04)** Migrations antigas (`InitialCreate`, `AddRoleToUsers`, `AddPasswordToUsers`, `AddUpdatedAtToUsers`) apagadas; schema passou a nascer via `db/init.sql` (script SQL idempotente), executado pelo serviço `sqlserver-init` no `docker-compose.yml` **antes** da `api` subir (`depends_on: condition: service_completed_successfully`). `Program.cs` **não chama mais** `dbContext.Database.MigrateAsync()`. O mesmo `db/init.sql` é reaproveitado pelos testes de integração (`DatabaseFixture`, via `Link` no `.csproj`) — variável `$(DatabaseName)` (sintaxe `sqlcmd`) é substituída por `Fiap_Workshop` no compose e por `IntegrationTestsDb` via `string.Replace` no `DatabaseFixture`.
- **(2026-08-04)** Modelos de infra criados para todos os agregados do domínio (`CustomerModel`, `VehicleModel`, `InventoryItemModel`, `ServiceModel`, `ServiceOrderModel` + `ServiceOrderPartModel`/`ServiceOrderServiceModel`/`ServiceOrderStatusHistoryModel` como filhos, com `HasMany().WithOne().OnDelete(Cascade)`), com `ModelMappers`/`DomainMappers` e configuração completa em `AppDbContext.OnModelCreating` (índices únicos em `Document/Email/Phone` do Customer, `LicensePlate`, `Code`, `Number`; FKs explícitas entre agregados sem navegação — ex. `Vehicle.CustomerId → Customers` — via `HasOne<T>().WithMany().HasForeignKey().OnDelete(Restrict)`).
- **(2026-08-07)** Migration baseline **regenerada do zero** (`20260807031339_InitialCreate`, via `dotnet ef migrations add --project src/Fiap.Workshop.Infrastructure --startup-project src/Fiap.Workshop.Api`) cobrindo todos os agregados de uma vez — a pasta `Migrations/` existe só como apoio de ferramenta (gera SQL a partir do modelo real, útil pra conferir/gerar o `db/init.sql`), não é a fonte de verdade em runtime.
- `Fiap.Workshop.Api.csproj` também referencia `Microsoft.EntityFrameworkCore.Design` (necessário porque a Api é o startup project usado pela ferramenta `dotnet ef`).

### Serviços
- `JwtService : IJwtService` — assinatura mudou para `GenerateToken(User user)` (antes recebia `string userId, email, role` soltos, ver §4); gera token HS256 com claims `ClaimTypes.NameIdentifier` (`user.Id`), `ClaimTypes.Email`, `ClaimTypes.Role` (`user.Role.ToString()`); lê `Jwt:SecretKey/Issuer/Audience/ExpirationInMinutes` da configuração (expiração com fallback de 60 min se a config não for um `int` válido).
- `CurrentUserService : ICurrentUserService` — lê claims do `IHttpContextAccessor`. Registrada desde sempre, mas **sem nenhum consumidor até `feature/8-put-users`** — o endpoint `PATCH /users/{id}/password` (ver §6) é o primeiro código da Api a injetá-la de verdade, pra checar se quem chama é o dono do `userId` da rota.
- `PasswordService : IPasswordService` — `BCrypt.Net.BCrypt.HashPassword`/`Verify`, work factor 12. Registrado como `Singleton` (stateless). Ver §10 — não é um Domain Service aqui, é um serviço técnico de Application/Infrastructure.
- `DomainEventDispatcher : IDomainEventDispatcher` — resolve `IDomainEventHandler<TEvent>` via reflection (`serviceProvider.GetServices` + `MakeGenericType`) e invoca `HandleAsync` em cada handler registrado. **Nenhum handler concreto existe ainda no DI** — hoje o dispatch é um no-op. Ver §11.

### Composição (`IoC/DependencyInjection.cs`)
`AddInfrastructure()`:
- `AddRepositories`: registra `AppDbContext` (SqlServer via `ConnectionStrings:DefaultConnection`) e os 6 repositórios (`IUserRepository`, `ICustomerRepository`, `IVehicleRepository`, `IInventoryItemRepository`, `IServiceRepository`, `IServiceOrderRepository`) como `Scoped`.
- `AddServices`: `IPasswordService` (`Singleton`), `ICurrentUserService` (`Scoped`), `IJwtService` (`Scoped`).
- `IDomainEventDispatcher` (`Scoped`), `IHttpContextAccessor`.

## 6. Camada de API (`Fiap.Workshop.Api`)

> ⚠️ Reescrita em 2026-08-11 — desde a última passada (2026-08-08) ganhou o endpoint de login (`Auth`) e o primeiro endpoint de um agregado de negócio (`Customers`, criação). Atualizada em 2026-08-12 (branch `feature/8-put-users`) com `PUT /users/{id}` e `PATCH /users/{id}/password`, em 2026-08-13 (branch `feature/9-user-delete`) com `DELETE /users/{id}`, no mesmo dia (branch `feature/10-update-customer`) com `PUT /customers/{id}`, em 2026-08-14/15 (branches `feature/11-create-vehicle`/`feature/12-get-vehicle`) com o grupo `Vehicles` inteiro (`POST`/`GET /{id}`) — primeiro grupo de endpoints de um agregado além de `Users`/`Customers` — em 2026-08-15 (branch `feature/13-create-service`) com o grupo `Services` (`POST`), no mesmo dia (branch `feature/14-get-services`) com `GET /services`, no mesmo dia (branch `feature/15-create-inventory-item`) com o grupo `InventoryItems` (`POST` apenas por enquanto), no mesmo dia (branch `feature/16-get-inventory-item`) com `GET /inventory-items`, no mesmo dia (branch `feature/17-create-service-order`) com o grupo `ServiceOrders` (`POST` apenas por enquanto) — primeiro endpoint de `ServiceOrder` — no mesmo dia (branch `feature/18-get-service-order`) com `GET /service-orders/{id}`, em 2026-08-16 (branch `feature/19-start-diagnosis`) com `PATCH /service-orders/{id}/diagnosis`, no mesmo dia (branch `feature/20-add-budget`) com `PATCH /service-orders/{id}/budget`, no mesmo dia (branch `feature/21-approve-service-order`) com `PATCH /service-orders/{id}/approve`, e no mesmo dia (branch `feature/22-reject-service-order`) com `PATCH /service-orders/{id}/reject`. `GetUserById` (o antigo scaffold) continua sem existir como rota própria — `GetUsers` (lista) e `UpdateUser` cobrem o essencial que ele faria.

Minimal APIs organizadas por feature em `Endpoints/{Feature}/*Endpoints.cs`, registradas em `Endpoints/EndpointsExtensions.cs` → `app.MapMinimalApisV1()`.

### Endpoints existentes

**Users** (`/api/v{version}/users`)
- `GET /` — `GetUsers` (novo, `feature/7-get-users`). Tem `.RequireAuthorization("AdminOnly")`. Sem parâmetro nenhum (nem query, nem rota, nem corpo) — o `CorrelationId` passado ao use case é gerado inline no próprio endpoint (`Guid.NewGuid()`), não vem do cliente. Sempre retorna `Results.Ok(result)` (**só declara `Produces<Output>(200)`, sem `400`**) — consistente com `GetUsersUseCase` nunca marcar `IsValid = false` (ver §4): lista vazia também é `200 OK` com resultado vazio.
- `POST /` — `CreateUser`. Tem `.RequireAuthorization("AdminOnly")` — **exige um token JWT válido com role `Admin`**. Não há `.AllowAnonymous()`. Na prática isso continua sendo um problema de bootstrap real: sem nenhum Admin pré-existente (não há seed no `db/init.sql`) e sem `AllowAnonymous` em `CreateUser`, **não há caminho pela API pra criar o primeiro usuário** — o endpoint de login (abaixo) resolve "logar depois de ter um usuário", não "criar o primeiro usuário". Ver §9. Valida com `CreateUserRequestValidator` via `.WithValidation<CreateUserRequest>()`, chama `ICreateUserUseCase`. Retorna `201 Created` (`Location` lendo `result.GetResult<UserResponse>()?.Id` — tipo certo, ver §9 item 2) com o `Output` (contendo o `UserResponse` em `Result`) ou `400 BadRequest` com o `Output` (lista de erros).
- `PUT /{userId}` — `UpdateUser` (novo, `feature/8-put-users`). Tem `.RequireAuthorization("AdminOnly")`. `userId` vem da rota (`[FromRoute] Guid`, `[Required]`); corpo é `UpdateUserRequest(CorrelationId, Name, Email, Role)`. Retorna sempre `400 BadRequest` pra qualquer falha do use case (usuário não existe **ou** e-mail já em uso) — **decisão consciente, não um descuido**: diferente do `GET /customers/{id}` (que usa `404` porque só tem um jeito de falhar), `UpdateUserUseCase` bota os dois motivos no mesmo `IsValid = false`/`ErrorMessages`, sem nenhum campo que distinga "não encontrado" de "conflito"; segui o mesmo padrão do `CreateUser`/`CreateCustomer` (sempre `400`) em vez de inspecionar o texto da mensagem de erro dentro do endpoint pra decidir o status. Se um dia isso importar, a mudança certa é o `Output` carregar um código de erro, não o endpoint interpretando string.
- `PATCH /{userId}/password` — `ChangePassword` (novo, `feature/8-put-users`). `userId` vem da rota; corpo é `ChangePasswordRequest(CorrelationId, CurrentPassword, NewPassword)`. **Único endpoint com `.RequireAuthorization()` sem policy nomeada** — todos os outros usam `AdminOnly`/`AttendantOnly`; aqui a regra não é sobre role (qualquer usuário autenticado, seja `Admin`/`Attendant`/`Mechanic`, pode trocar a própria senha), é sobre identidade. Por isso o handler também injeta `ICurrentUserService` (**primeiro consumidor real dela na camada de Api** — antes só existia registrada no DI, sem nada usando) e compara `currentUser.UserId != userId` da rota, devolvendo `403 Forbidden` (`Results.Forbid()`) se não bater — essa checagem vive só no endpoint, não no use case (`ChangePasswordUseCase` trocaria a senha de qualquer `UserId` que receber; quem impõe "só a sua própria" é a Api). Fora isso, `400 BadRequest` pra "usuário não existe"/"senha atual inválida" (mesmo raciocínio de status único do `PUT`, acima), `200 OK` em sucesso.
- `DELETE /{userId}` — `DeleteUser` (novo, `feature/9-user-delete`). Tem `.RequireAuthorization("AdminOnly")`. `userId` vem da rota (`[FromRoute] Guid`, `[Required]`); sem corpo — `CorrelationId` é gerado inline (`Guid.NewGuid()`), mesmo padrão dos `GET` sem corpo (`GetUsers`/`GetCustomer`). **Primeiro (e único até agora) endpoint a devolver `204 No Content`** em vez de `200 OK`/`Output` no caminho de sucesso — decisão tomada junto com o use case (ver §4): como `DeleteUserUseCase` trata "não encontrado" como sucesso idempotente (`IsValid = true`, sem `AddErrorMessage`), o endpoint devolve `204 No Content` tanto quando o usuário existia e foi removido quanto quando já não existia; só cai em `400 BadRequest` com o `Output` se o `CommitAsync` falhar de verdade (erro de persistência). Diferente de `PUT`/`PATCH` acima, aqui o `Output` de sucesso não tem `Result`/`Messages` pra devolver, então não faz sentido embrulhar num `200` — por isso a rota quebra o padrão "sempre `Results.Ok(result)`" usado no resto do arquivo.

**Auth** (`/api/v{version}/auth`) — novo desde a última reescrita
- `POST /login` — `LoginUser`. Tem `.AllowAnonymous()` — não exige autenticação (faz sentido, é o próprio ponto de entrada). Valida com `LoginRequestValidator` via `.WithValidation<LoginRequest>()`, chama `ILoginUserUseCase`. Retorna `200 OK` com o `Output` (token cru em `Result`) se válido, ou `401 Unauthorized` **sem corpo** se inválido (diferente do padrão `400 BadRequest` + `Output` usado nos outros endpoints — aqui o `Output`/lista de erros do use case é descartado).

**Customers** (`/api/v{version}/customers`)
- `GET /{customerId}` — `GetCustomer` (existia antes desta branch, não documentado até agora). Tem `.RequireAuthorization("AttendantOnly")`. `customerId` vem da rota (`[FromRoute] Guid`); `CorrelationId` é gerado inline (`Guid.NewGuid()`), igual ao `GET /users`. Retorna `404 NotFound` com o `Output` se `IsValid == false`, senão `200 OK`.
- `POST /` — `CreateCustomer`. Tem `.RequireAuthorization("AttendantOnly")` (roles `Attendant`/`Admin`) — **primeiro endpoint a usar uma policy diferente de `AdminOnly`**. Valida com `CreateCustomerRequestValidator` via `.WithValidation<CreateCustomerRequest>()`, chama `ICreateCustomerUseCase`. Retorna `201 Created` (`Location` lendo `result.GetResult<CustomerResponse>()?.Id` — tipo certo, ver §9 item 2) com o `Output` ou `400 BadRequest` com o `Output`.
- `PUT /{customerId}` — `UpdateCustomer` (novo, `feature/10-update-customer`). Tem `.RequireAuthorization("AttendantOnly")`, mesma policy do `GET`/`POST` acima. `customerId` vem da rota (`[FromRoute] Guid`, `[Required]`); corpo é `UpdateCustomerRequest(CorrelationId, Name, Email, Phone)` — **sem `Document`**, ver §3/§4. Retorna sempre `400 BadRequest` pra qualquer falha do use case (customer não existe, e-mail já em uso, ou telefone já em uso — três motivos, não dois), mesmo raciocínio do `PUT /users/{id}` (status único, sem inspecionar a mensagem de erro pra decidir o código).

**Vehicles** (`/api/v{version}/vehicles`) — novo, `feature/11-create-vehicle`/`feature/12-get-vehicle`, primeiro grupo além de `Users`/`Customers`
- `POST /` — `CreateVehicle`. Tem `.RequireAuthorization("AttendantOnly")` — mesma policy do `CreateCustomer` (é o atendente cadastrando o veículo do cliente). Valida com `CreateVehicleRequestValidator` via `.WithValidation<CreateVehicleRequest>()`, chama `ICreateVehicleUseCase`. Retorna `201 Created` (`Location` lendo `result.GetResult<VehicleResponse>()?.Id`) ou `400 BadRequest`. **Pode devolver `400` por três motivos diferentes**: erro de validação de campo (via `WithValidation`), regra de negócio do use case (customer não existe / placa duplicada), ou uma `DomainException` do agregado (`ModelYear < ManufactureYear`) capturada pelo `DomainExceptionHandler` global (ver abaixo) — os três caem no mesmo formato de resposta (`400` + `Output`), então o cliente não percebe a diferença de origem.
- `GET /{vehicleId}` — `GetVehicle`. Tem `.RequireAuthorization("AttendantOnly")`. `vehicleId` vem da rota; `CorrelationId` gerado inline, mesmo padrão do `GET /customers/{id}`. Retorna `404 NotFound` com o `Output` se `IsValid == false`, senão `200 OK` — mesmo padrão do `GetCustomer` (não `400`, diferente de `GetUsers`/`DeleteUser`).

**Services** (`/api/v{version}/services`) — novo, `feature/13-create-service`/`feature/14-get-services`
- `GET /` — `GetServices` (novo, `feature/14-get-services`). Tem `.RequireAuthorization()` **sem policy nomeada** — decisão confirmada com o usuário: consulta de catálogo deve ser acessível pelos três papéis (`Attendant`/`Mechanic`/`Admin`), e como não existe um quarto role no sistema, "qualquer usuário autenticado" (`.RequireAuthorization()` puro) produz exatamente esse efeito sem precisar de uma policy nova — mesmo mecanismo já usado por `PATCH /users/{id}/password` (ver acima), só que ali por motivo de identidade, aqui por não haver necessidade de restringir por role. Sem parâmetro nenhum — `CorrelationId` gerado inline (`Guid.NewGuid()`), mesmo padrão do `GET /users`. Sempre `Results.Ok(result)` (só `Produces<Output>(200)`, sem `400`) — `GetServicesUseCase` nunca marca `IsValid = false` (ver §4).
- `POST /` — `CreateService`. Tem `.RequireAuthorization("AdminOnly")` — decisão confirmada com o usuário (catálogo/precificação é dado administrativo, diferente do cadastro operacional de `Customer`/`Vehicle`). Valida com `CreateServiceRequestValidator` via `.WithValidation<CreateServiceRequest>()`, chama `ICreateServiceUseCase`. Retorna `201 Created` (`Location` lendo `result.GetResult<ServiceResponse>()?.Id`) ou `400 BadRequest`. **Pode devolver `400` por três motivos**, mesmo racional do `CreateVehicle`: erro de validação de campo, código duplicado (regra de negócio do use case), ou `DomainException` do agregado (`BasePrice`/`EstimatedDuration` negativos) capturada pelo `DomainExceptionHandler` global.

**InventoryItems** (`/api/v{version}/inventory-items`) — `feature/15-create-inventory-item`/`feature/16-get-inventory-item`
- `GET /` — `GetInventoryItems` (novo, `feature/16-get-inventory-item`). Tem `.RequireAuthorization()` **sem policy nomeada** — mesma decisão do `GET /services` (consulta de estoque acessível pelos três papéis, sem motivo de negócio pra restringir por role). Sem parâmetro nenhum — `CorrelationId` gerado inline (`Guid.NewGuid()`), mesmo padrão do `GET /services`/`GET /users`. Sempre `Results.Ok(result)` (só `Produces<Output>(200)`, sem `400`) — `GetInventoryItemsUseCase` nunca marca `IsValid = false`.
- `POST /` — `CreateInventoryItem`. Tem `.RequireAuthorization("AdminOnly")` — mesma policy do `CreateService`, por analogia (catálogo de peças/insumos com preço, mesma categoria de decisão que serviço). Valida com `CreateInventoryItemRequestValidator` via `.WithValidation<CreateInventoryItemRequest>()`, chama `ICreateInventoryItemUseCase`. Retorna `201 Created` (`Location` lendo `result.GetResult<InventoryItemResponse>()?.Id`) ou `400 BadRequest`. **Pode devolver `400` por três motivos**, mesmo racional do `CreateService`: erro de validação de campo, código duplicado, ou `DomainException` do agregado (`QuantityOnHand`/`MinimumStock`/`UnitPrice` negativos, ou `ReservedQuantity > QuantityOnHand`) capturada pelo `DomainExceptionHandler` global.

**ServiceOrders** (`/api/v{version}/service-orders`) — `feature/17-create-service-order`/`feature/18-get-service-order`/`feature/19-start-diagnosis`/`feature/20-add-budget`/`feature/21-approve-service-order`/`feature/22-reject-service-order`
- `GET /{serviceOrderId}` — `GetServiceOrder` (novo, `feature/18-get-service-order`). Tem `.RequireAuthorization()` **sem policy nomeada** — diferente do `GET /vehicles/{id}`/`GET /customers/{id}` (`AttendantOnly`): uma OS é consultada pelos três papéis em pontos diferentes do fluxo (`Attendant` abre/aprova/entrega, `Mechanic` diagnostica/orça/conclui, ver `docs/backlog-proximas-tarefas.md`), então não faz sentido restringir a leitura a um papel só — mesmo racional do `GET /services`/`GET /inventory-items` (consulta ampla), só que aqui por divisão de fluxo entre papéis, não por ser "catálogo". `serviceOrderId` vem da rota; `CorrelationId` gerado inline. Retorna `404 NotFound` com o `Output` se `IsValid == false` (mesmo padrão do `GetVehicle`/`GetCustomer`, não `400`), senão `200 OK` com `Parts`/`Services`/`StatusHistory` populados.
- `POST /` — `CreateServiceOrder`. Tem `.RequireAuthorization("AttendantOnly")` — mesma policy do `CreateCustomer`/`CreateVehicle` (é o atendente que recebe o veículo e abre a OS na recepção). Injeta `ICurrentUserService` **direto no delegate do endpoint** (mesmo padrão do `PATCH /users/{id}/password`, ver acima) só para ler `currentUser.UserId` e passar pro mapper como `CreatedBy` — `request.MapToInput(currentUser.UserId)`, mesmo molde do `UpdateUserMapper.MapToInput(request, userId)` (parâmetro extra vindo de fora do corpo, só que aqui vem do token JWT, não da rota). Valida com `CreateServiceOrderRequestValidator` via `.WithValidation<CreateServiceOrderRequest>()`, chama `ICreateServiceOrderUseCase`. Retorna `201 Created` (`Location` lendo `result.GetResult<ServiceOrderResponse>()?.Id`) ou `400 BadRequest`. **Pode devolver `400` por quatro motivos**: erro de validação de campo, cliente não existe, veículo não existe, ou veículo não pertence ao cliente informado — mais que `CreateVehicle`/`CreateService` porque valida duas referências cruzadas em vez de uma.
- `PATCH /{serviceOrderId}/diagnosis` — `StartDiagnosis` (novo, `feature/19-start-diagnosis`, ticket 19). Tem `.RequireAuthorization("MechanicOnly")` — **primeiro consumidor real dessa policy** (registrada desde `feature/2` mas sem nenhum endpoint até aqui). `serviceOrderId` vem da rota; corpo é `StartDiagnosisRequest(CorrelationId, DiagnoseDescription)` — **sem `ChangedBy`**, mesmo racional do `CreatedBy` em `CreateServiceOrder`: injeta `ICurrentUserService` direto no delegate do endpoint (`request.MapToInput(serviceOrderId, currentUser.UserId)`). Valida com `StartDiagnosisRequestValidator` via `.WithValidation<StartDiagnosisRequest>()`, chama `IStartDiagnosisUseCase`. Retorna `200 OK` (não `201`, é mutação de um recurso existente) ou `400 BadRequest` — mesmo padrão único-status do `PUT /users/{id}` (não distingue "OS não encontrada" de "transição de status inválida" no código HTTP; a segunda vem de uma `DomainException` capturada pelo `DomainExceptionHandler` global, primeira vez que isso acontece pra `ServiceOrder`).
- `PATCH /{serviceOrderId}/budget` — `AddBudget` (novo, `feature/20-add-budget`, ticket 20). Tem `.RequireAuthorization("MechanicOnly")`, mesma policy do `StartDiagnosis` (mesmo papel monta diagnóstico e orçamento). `serviceOrderId` vem da rota; corpo é `AddBudgetRequest(CorrelationId, Services: IReadOnlyCollection<AddBudgetServiceItemRequest>, Parts: IReadOnlyCollection<AddBudgetPartItemRequest>)` — **sem `ChangedBy`** (mesmo racional dos outros dois endpoints de transição) **e sem `Discount`** (ver §3 — decisão revista: desconto não faz parte de montar o orçamento, fica pra um use case futuro ainda não desenhado). Valida com `AddBudgetRequestValidator` via `.WithValidation<AddBudgetRequest>()` — primeiro validator do projeto a validar coleções (`RuleForEach` + `ChildRules` pra `ServiceId`/`InventoryItemId` obrigatórios e `Quantity > 0` em cada item). Retorna `200 OK` ou `400 BadRequest`. **Pode devolver `400` por quatro motivos**: erro de validação de campo, OS não encontrada, peça não encontrada, serviço não encontrado, ou uma `DomainException` (transição de status inválida ou estoque insuficiente) capturada pelo `DomainExceptionHandler` global.
- `PATCH /{serviceOrderId}/approve` — `ApproveServiceOrder` (novo, `feature/21-approve-service-order`, ticket 21). Tem `.RequireAuthorization("AttendantOnly")` — diferente dos outros dois `PATCH` de transição (`MechanicOnly`): é o atendente quem registra a decisão do cliente, não o mecânico (decisão já fechada no backlog, ver §4 do `docs/backlog-proximas-tarefas.md`). `serviceOrderId` vem da rota; corpo é um `Guid` cru (`[FromBody] Guid request`, o próprio `CorrelationId`) — **sem wrapper record**, simplificado pelo usuário depois da primeira versão (que tinha `ApproveServiceOrderRequest(CorrelationId)`, um record só com esse campo — removido por não agregar nada sobre o `Guid` puro). Mapper: `ApproveServiceOrderMapper.MapToInput(this Guid request, Guid serviceOrderId, Guid changedBy)` — estende `Guid` diretamente. **Sem `.WithValidation<TRequest>()`** — de propósito, não por descuido: o filtro genérico `ValidationFilter<TRequest>` (ver abaixo) resolve o argumento a validar via `context.Arguments.OfType<TRequest>().FirstOrDefault()`, e esse endpoint tem **dois** parâmetros `Guid` (`serviceOrderId` da rota e `request` do corpo) — `OfType<Guid>().FirstOrDefault()` pegaria sempre `serviceOrderId` (primeiro na assinatura do delegate), nunca o `request` do corpo, então um `.WithValidation<Guid>()` aqui validaria o campo errado silenciosamente. Por isso o endpoint não tenta validar `CorrelationId` vazio nesse caso — inconsistente com os outros `PATCH` de transição (que exigem `CorrelationId` não-vazio via `NotEmpty()`), mas o trade-off foi considerado aceitável dado o risco de validar a coisa errada. Chama `IApproveServiceOrderUseCase`. Retorna `200 OK` ou `400 BadRequest`. **Pode devolver `400` por três motivos** (não passa por validação de campo): OS não encontrada, `InventoryItem` não encontrado, ou uma `DomainException` (transição de status inválida ou tentativa de debitar mais do que o reservado) capturada pelo `DomainExceptionHandler` global.
- `PATCH /{serviceOrderId}/reject` — `RejectServiceOrder` (novo, `feature/22-reject-service-order`, ticket 22). Tem `.RequireAuthorization("AttendantOnly")` — mesma policy do `approve` (é o atendente quem registra a decisão do cliente, aprovando ou reprovando). **Estrutura idêntica ao endpoint `/approve`**: `serviceOrderId` vem da rota; corpo é um `Guid` cru (`[FromBody] Guid request`); mapper `RejectServiceOrderMapper.MapToInput(this Guid request, Guid serviceOrderId, Guid changedBy)`, mesmo molde do `ApproveServiceOrderMapper`; sem `.WithValidation<TRequest>()` pelo mesmo motivo (dois `Guid` na assinatura do delegate colidiriam com o filtro genérico). Chama `IRejectServiceOrderUseCase`. Retorna `200 OK` ou `400 BadRequest`. **Pode devolver `400` pelos mesmos três motivos do `/approve`**: OS não encontrada, `InventoryItem` não encontrado, ou uma `DomainException` (transição de status inválida ou tentativa de liberar mais do que o reservado) capturada pelo `DomainExceptionHandler` global. **Achado durante a implementação**: `ApproveServiceOrderMapper.MapToInput` e `RejectServiceOrderMapper.MapToInput` têm a mesma assinatura de extensão (`this Guid, Guid, Guid`), as duas classes moram no mesmo namespace (`Fiap.Workshop.Api.Mappers`), e o arquivo `ServiceOrdersEndpoints.cs` importa esse namespace uma vez pro grupo inteiro — assim que as duas rotas passaram a coexistir no mesmo arquivo, a sintaxe de extensão (`request.MapToInput(...)`) virou ambígua (`CS0121`, "a chamada é ambígua entre ... ApproveServiceOrderMapper.MapToInput ... e RejectServiceOrderMapper.MapToInput"). Resolvido chamando os dois mappers pelo nome da classe explicitamente (`ApproveServiceOrderMapper.MapToInput(request, serviceOrderId, currentUser.UserId)`/`RejectServiceOrderMapper.MapToInput(...)`) nos dois endpoints, em vez de sintaxe de extensão — evita a ambiguidade sem precisar de um wrapper record em nenhum dos dois. Vale lembrar disso se um terceiro endpoint de transição sem payload (`Guid` cru) for adicionado no mesmo grupo.

`CorrelationId` (`Guid`, `NotEmpty`) é um **campo do corpo** em todo request que tem corpo (`CreateUserRequest`, `LoginRequest`, `CreateCustomerRequest`, `UpdateUserRequest`, `ChangePasswordRequest`, `UpdateCustomerRequest`, `CreateVehicleRequest`, `CreateServiceRequest`, `CreateInventoryItemRequest`, `CreateServiceOrderRequest` — `POST`/`PUT`/`PATCH`), não um header — não existe leitura de `x-correlation-id` em lugar nenhum do código atual. Nas rotas sem corpo (`GetUsers`, `GetCustomer`, `GetVehicle`, `DELETE /users/{id}`), o padrão é diferente: o próprio endpoint gera um `Guid.NewGuid()` inline na chamada ao use case, o cliente não tem como fornecer/propagar um `CorrelationId` nessas rotas.

### Tratamento global de erro de domínio (`Handlers/DomainExceptionHandler.cs`, novo, `feature/11-create-vehicle`)

`DomainExceptionHandler : IExceptionHandler` (contrato nativo do ASP.NET Core, não é o `ValidationFilter` de FluentValidation) — registrado via `services.AddExceptionHandler<DomainExceptionHandler>()` (`Api/IoC/DependencyInjection.cs`, antes do `AddProblemDetails()`), consumido pelo `app.UseExceptionHandler()` já existente no `Program.cs`. Qualquer `DomainException` (`Domain.Abstractions`) não capturada localmente por um use case é interceptada aqui: seta `400 BadRequest` e escreve um `Output` (`Application.Commons`) com `AddErrorMessage(ex.Message)` no corpo — mesmo formato de resposta que todo o resto da Api usa pra erro de negócio. Exceções que não são `DomainException` continuam caindo no comportamento padrão do `UseExceptionHandler()` (hoje sem handler customizado, geralmente vira `500`).

**Por que isso existe**: antes desse handler, uma `DomainException` estourando de dentro de um use case (ex.: `new Vehicle(...)` com `ModelYear < ManufactureYear`) não tinha nenhum tratamento — o cliente recebia `500 Internal Server Error` genérico em vez de um `400` de regra de negócio. Isso não tinha sido notado antes porque nenhum use case até `CreateVehicle` construía uma entidade que valida algo no construtor (`ServiceOrderPart`/`ServiceOrderService` já validavam, mas não tinham use case nenhum consumindo). Resolvido de uma vez pra todos os agregados presentes e futuros, em vez de um `try/catch` local em cada use case — decisão consciente de manter os use cases livres desse tipo de boilerplate.

Sem cobertura de teste dedicada ainda: exigiria referenciar o projeto Api nos testes unitários (hoje `Fiap.Workshop.UnitTests` não referencia `Fiap.Workshop.Api`) ou usar `Fiap.Workshop.FunctionalTests` (vazio, ver §9 item 5) — registrado como gap pequeno, não bloqueante.

Não existe grupo próprio nem rota para `UpdateEmailRequest` (`Requests/Users/`) — segue como record órfão, sem mapper, validator, use case ou rota.

### Requests / Validators / Mappers
- `CreateUserRequest(CorrelationId, Name, Email, Password, PasswordConfirmation, Role)` — validado por `CreateUserRequestValidator`: `CorrelationId` obrigatório; `Name` obrigatório (≤200); `Email` obrigatório + `.EmailAddress()` (sem limite de tamanho explícito no validator); `Password` obrigatório, mínimo 8 caracteres (**sem** checar maiúscula/minúscula/dígito); `PasswordConfirmation` igual a `Password`; `Role` dentro do enum (`Admin`/`Attendant`/`Mechanic`, ver §3). Não há nenhuma validação equivalente no Domain (ver §10) — hoje é só a API que garante isso, sem "defesa em profundidade". Mapper: `CreateUserMapper.MapToInput` (`Api/Mappers/`).
- `UpdateUserRequest(CorrelationId, Name, Email, Role)` (novo, `feature/8-put-users`) — validado por `UpdateUserRequestValidator`: mesmas regras de `CreateUserRequestValidator` menos as de senha (não existe campo `Password` aqui). Mapper: `UpdateUserMapper.MapToInput(this UpdateUserRequest request, Guid userId)` (`Api/Mappers/`) — assinatura diferente dos outros mappers HTTP↔Application porque o `userId` vem da rota, não do corpo, e precisa ser combinado com o request pra montar o `UpdateUserInput`.
- `ChangePasswordRequest(CorrelationId, CurrentPassword, NewPassword)` (novo, `feature/8-put-users`) — validado por `ChangePasswordRequestValidator`: `CorrelationId`/`CurrentPassword` obrigatórios; `NewPassword` obrigatório + mínimo 8 caracteres (mesma regra frouxa do `CreateUserRequestValidator`, sem checar maiúscula/minúscula/dígito; **sem** checar `NewPassword != CurrentPassword`, omissão consciente, não pedida). Mapper: `ChangePasswordMapper.MapToInput(this ChangePasswordRequest request, Guid userId)` — mesmo molde do `UpdateUserMapper` (`userId` da rota).
- `LoginRequest(CorrelationId, Email, Password)` — validado por `LoginRequestValidator`: `CorrelationId` obrigatório; `Email` obrigatório + `.EmailAddress()`; `Password` obrigatório, mínimo 8 caracteres. Mapper: `LoginUserMapper.MapToInput` (`Api/Mappers/`).
- `CreateCustomerRequest(CorrelationId, Name, Document, Email, Phone)` — validado por `CreateCustomerRequestValidator`: `CorrelationId` obrigatório; `Name` obrigatório (≤200); `Document` obrigatório, `Cascade(CascadeMode.Stop)` + tamanho 11 ou 14 + `IsValidDocument()` (dígito verificador real de CPF/CNPJ, via `CpfCnpjLibrary` — ver §2/§4); `Email` obrigatório + `.EmailAddress()`. **`Phone` não tem nenhuma regra de validação** (nem `NotEmpty`) — só existe validação de CPF/CNPJ, não de telefone, hoje. Mapper: `CreateCustomerMapper.MapToInput` (`Api/Mappers/`).
- `UpdateCustomerRequest(CorrelationId, Name, Email, Phone)` (novo, `feature/10-update-customer`) — **sem `Document`**, ver §3/§4/§6. Validado por `UpdateCustomerRequestValidator`: `CorrelationId`/`Name`(≤200)/`Email` seguem as mesmas regras de `CreateCustomerRequestValidator` (sem regras de `Document`, que não existe aqui). **`Phone` continua sem nenhuma regra de validação**, mesma omissão do `CreateCustomerRequestValidator` — não é uma inconsistência nova, é a mesma lacuna existente se propagando pro update. Mapper: `UpdateCustomerMapper.MapToInput(this UpdateCustomerRequest request, Guid customerId)` — mesmo molde do `UpdateUserMapper` (`customerId` da rota).
- `CreateVehicleRequest(CorrelationId, CustomerId, LicensePlate, Brand, Model, ManufactureYear, ModelYear, Color)` (novo, `feature/11-create-vehicle`) — validado por `CreateVehicleRequestValidator`: `CorrelationId`/`CustomerId` obrigatórios; `LicensePlate` obrigatório + `IsValidLicensePlate()` (formato Mercosul/antigo, ver §4); `Brand`/`Model` obrigatórios (≤100); `Color` obrigatório (≤50); `ManufactureYear > 0`; `ModelYear >= ManufactureYear` (**redundante de propósito** com a invariante do agregado `Vehicle`, ver §3/§4 — validator dá erro de campo específico com boa UX, domínio garante a regra pra qualquer código que não passe pelo validator). Mapper: `CreateVehicleMapper.MapToInput` (`Api/Mappers/`).
- `CreateServiceRequest(CorrelationId, Code, Name, Description, BasePrice, EstimatedDuration)` (novo, `feature/13-create-service`) — validado por `CreateServiceRequestValidator`: `CorrelationId` obrigatório; `Code` obrigatório (≤50, mesmo tamanho do índice único da coluna, ver §5); `Name` obrigatório (≤200); `Description` obrigatório (≤1000); `BasePrice >= 0`; `EstimatedDuration >= 0` — **redundante de propósito** com a invariante do agregado `Service` (ver §3/§4), mesmo racional do `ModelYear`/`ManufactureYear` do `Vehicle`. Mapper: `CreateServiceMapper.MapToInput` (`Api/Mappers/`).
- `CreateInventoryItemRequest(CorrelationId, Code, Name, Description, QuantityOnHand, MinimumStock, UnitPrice, UnitOfMeasure)` (novo, `feature/15-create-inventory-item`) — validado por `CreateInventoryItemRequestValidator`: `CorrelationId` obrigatório; `Code` obrigatório (≤50); `Name` obrigatório (≤200); `Description` obrigatório (≤1000); `QuantityOnHand >= 0`; `MinimumStock >= 0`; `UnitPrice >= 0` — **redundante de propósito** com as invariantes do agregado `InventoryItem` (ver §3/§4); `UnitOfMeasure` via `.IsInEnum()`, mesmo padrão do `Role` em `CreateUserRequestValidator`. Mapper: `CreateInventoryItemMapper.MapToInput` (`Api/Mappers/`).
- `CreateServiceOrderRequest(CorrelationId, CustomerId, VehicleId, ProblemDescription, OdometerReading)` (novo, `feature/17-create-service-order`) — **sem `CreatedBy`**, decisão de negócio consciente (ver §4: vem de `ICurrentUserService`, não é confiável vindo do corpo). Validado por `CreateServiceOrderRequestValidator`: `CorrelationId`/`CustomerId`/`VehicleId` obrigatórios; `ProblemDescription` obrigatório (≤2000, mesmo tamanho da coluna); `OdometerReading >= 0`. Mapper: `CreateServiceOrderMapper.MapToInput(this CreateServiceOrderRequest request, Guid createdBy)` — assinatura com parâmetro extra, mesmo molde do `UpdateUserMapper`/`ChangePasswordMapper` (só que aqui o parâmetro vem do `ICurrentUserService` injetado no endpoint, não da rota).
- `StartDiagnosisRequest(CorrelationId, DiagnoseDescription)` (novo, `feature/19-start-diagnosis`) — **sem `ServiceOrderId`** (vem da rota) **nem `ChangedBy`** (vem de `ICurrentUserService`), mesmo racional do `CreateServiceOrderRequest`. Validado por `StartDiagnosisRequestValidator`: `CorrelationId` obrigatório; `DiagnoseDescription` obrigatório (≤2000, mesmo tamanho de `DiagnoseDescription` na coluna). Mapper: `StartDiagnosisMapper.MapToInput(this StartDiagnosisRequest request, Guid serviceOrderId, Guid changedBy)` — dois parâmetros extras (rota + token), mesmo molde combinado do `UpdateUserMapper` (rota) e do `CreateServiceOrderMapper` (token).
- `AddBudgetRequest(CorrelationId, Services, Parts)` (novo, `feature/20-add-budget`) — `Services: IReadOnlyCollection<AddBudgetServiceItemRequest(ServiceId, Quantity)>`, `Parts: IReadOnlyCollection<AddBudgetPartItemRequest(InventoryItemId, Quantity)>` (records auxiliares no mesmo arquivo). **Sem `ServiceOrderId`/`ChangedBy`**, mesmo racional do `StartDiagnosisRequest`, **e sem `Discount`** (removido logo após a primeira versão — ver §3/§4, desconto não é escopo deste use case). Validado por `AddBudgetRequestValidator`: `CorrelationId` obrigatório; `Services`/`Parts` não-nulos; cada item de `Services` com `ServiceId` obrigatório + `Quantity > 0` (via `RuleForEach`/`ChildRules` — primeiro validator do projeto a validar uma coleção), mesma regra pra cada item de `Parts` com `InventoryItemId`. Mapper: `AddBudgetMapper.MapToInput(this AddBudgetRequest request, Guid serviceOrderId, Guid changedBy)` — projeta as listas de request pras listas de `AddBudgetServiceItem`/`AddBudgetPartItem` da Application, mesmo molde combinado do `StartDiagnosisMapper`.
- `ApproveServiceOrder` **não tem um record de request** — o corpo é um `Guid` cru (o `CorrelationId` em si), sem `ServiceOrderId` (vem da rota) nem `ChangedBy` (vem de `ICurrentUserService`). Rota de transição sem `.WithValidation<TRequest>()` (ver §6 acima, endpoint) — tem um `Guid` a mais na assinatura do delegate (`serviceOrderId` da rota) que colide com o filtro de validação genérico. Mapper: `ApproveServiceOrderMapper.MapToInput(this Guid request, Guid serviceOrderId, Guid changedBy)` — estende `Guid` diretamente em vez de um record.
- `RejectServiceOrder` (novo, `feature/22-reject-service-order`) — **mesma estrutura do `ApproveServiceOrder`** (corpo `Guid` cru, sem `.WithValidation<TRequest>()`, mesmo motivo). Mapper: `RejectServiceOrderMapper.MapToInput(this Guid request, Guid serviceOrderId, Guid changedBy)` — mesma assinatura do `ApproveServiceOrderMapper`, o que forçou as duas chamadas em `ServiceOrdersEndpoints.cs` a usarem a sintaxe de nome de classe explícito em vez de sintaxe de extensão (ver §6 acima, endpoint `/reject`, pra o `CS0121` que isso causou).
- `ValidationFilter<TRequest>` (`Filters/`) — `IEndpointFilter` genérico: resolve `IValidator<TRequest>` do DI, roda `ValidateAsync`, e se inválido retorna `400 BadRequest` com um `Output` (`Application.Commons`) preenchido via `AddErrorMessages`. Aplicado via `.WithValidation<TRequest>()` (`EndpointFilterExtensions`).

### Composição (`IoC/DependencyInjection.cs`)
`AddApi()`: `AddValidators` (registra `IValidator<CreateUserRequest>`, `IValidator<UpdateUserRequest>`, `IValidator<ChangePasswordRequest>`, `IValidator<LoginRequest>`, `IValidator<CreateCustomerRequest>`, `IValidator<UpdateCustomerRequest>`, `IValidator<CreateVehicleRequest>`, `IValidator<CreateServiceRequest>`, `IValidator<CreateInventoryItemRequest>`, `IValidator<CreateServiceOrderRequest>`, `IValidator<StartDiagnosisRequest>` e `IValidator<AddBudgetRequest>`, todos `Scoped` — **sem** `IValidator<ApproveServiceOrderRequest>`, esse tipo não existe mais, ver §6), `AddRoles` (`AddAuthorizationBuilder` com as três policies `AdminOnly`/`AttendantOnly`/`MechanicOnly`, ver §2 — hoje `AdminOnly` (`CreateUser`/`UpdateUser`/`DeleteUser`/`CreateService`/`CreateInventoryItem`), `AttendantOnly` (`CreateCustomer`/`UpdateCustomer`/`CreateVehicle`/`GetVehicle`/`CreateServiceOrder`/`ApproveServiceOrder`) e `MechanicOnly` (`PATCH /service-orders/{id}/diagnosis` e `PATCH /service-orders/{id}/budget`) são exercitadas por vários endpoints; `.RequireAuthorization()` sem policy nomeada — equivalente a "qualquer um dos três roles" — tem quatro consumidores: `PATCH /users/{id}/password`, `GET /services`, `GET /inventory-items` e `GET /service-orders/{id}`), `AddJwtConfig` (JWT Bearer lendo `Jwt:Issuer/Audience/SecretKey`, `AddApiVersioning` com URL segment reader e versão default 1 + `AddApiExplorer` com `SubstituteApiVersionInUrl = true`, `AddExceptionHandler<DomainExceptionHandler>()` (novo, ver acima — registrado **antes** de `AddProblemDetails()`), `AddProblemDetails`, OpenAPI com security scheme Bearer). **Fix aplicado em 2026-08-11** (commit `fe845b2`, "fix(ioc): dependency injection bearer fix"): o documento OpenAPI declarava o `SecurityScheme` "Bearer" nos `Components`, mas não adicionava o `document.Security` global exigindo esse scheme — o botão "Authorize" do Swagger UI aparecia, mas o token informado não era de fato anexado às requisições de teste feitas pela UI. Corrigido adicionando `document.Security.Add(new OpenApiSecurityRequirement { [Bearer] = [] })` no mesmo `AddDocumentTransformer`.

### `Program.cs`
Pipeline: `AddControllers` (não usado, pois tudo é Minimal API — resquício de template), `AddEndpointsApiExplorer`, `AddApplication → AddInfrastructure → AddApi`, `MapOpenApi + UseSwaggerUI` (docs em `/swagger`, aponta pro JSON em `/openapi/v1.json`), `UseExceptionHandler`, `UseHttpsRedirection`, `UseAuthentication`, `UseAuthorization`, `UseSerilogRequestLogging`, `MapMinimalApisV1()`.

## 7. Docker / Deploy

- **Dockerfile**: multi-stage (`sdk:10.0` restore → publish → `aspnet:10.0` runtime), copia apenas os `.csproj` primeiro (cache de layers), expõe porta `8080`.
- **docker-compose.yml**: serviços `api` (build local) + `sqlserver` (`mssql/server:2022-latest`) + `sqlserver-init` (2026-08-03, novo), healthcheck via `sqlcmd`, volume nomeado `sqlserver-data`. Variáveis vêm do `.env`.
- **`sqlserver-init`** (2026-08-03): serviço descartável (`restart: "no"`) que roda a mesma imagem `mssql/server:2022-latest` com `entrypoint` sobrescrito pra executar `/opt/mssql-tools18/bin/sqlcmd -i /init.sql` (monta `db/init.sql` como volume) contra o serviço `sqlserver` assim que ele fica `healthy`. `api` agora depende de `sqlserver-init` com `condition: service_completed_successfully` (não mais de `sqlserver: service_healthy` diretamente) — o banco já está com schema pronto antes da API subir.
- **docker-compose.tests.yml**: SQL Server isolado (`integration-sql`) para os testes de integração, senha fixa `Integration@Test123`. **Não** tem serviço de init próprio — quem aplica o schema é a `DatabaseFixture` (C#), lendo o mesmo `db/init.sql` compartilhado. Importante: **não há nenhum seed de dados** nesse fluxo — `db/init.sql` só tem `CREATE TABLE`/`CREATE INDEX`, nenhum `INSERT`. Cada classe de teste de integração é responsável por inserir os próprios dados no Arrange (via use case real ou repositório direto); como `DatabaseFixture` é `ICollectionFixture` compartilhado por toda a collection `"Integration"` sem reset entre testes, cenários que dependem da tabela estar vazia não são confiáveis nesse nível (ver `GetUsers`, §4).
- **.env / .env.example** (variáveis, sem valores sensíveis aqui): `ASPNETCORE_ENVIRONMENT`, `API_PORT`, `SA_PASSWORD`, `ConnectionStrings__DefaultConnection`, `Jwt__SecretKey`, `Jwt__Issuer`, `Jwt__Audience`, `Jwt__ExpirationInMinutes`. ⚠️ `.env` está versionado com uma senha/segredo de exemplo — confirmar se deveria estar no `.gitignore`. Nome do database (`Fiap_Workshop`) está hardcoded tanto na connection string quanto no `-v DatabaseName=Fiap_Workshop` do `sqlserver-init` — se um dia virar variável, precisa mudar nos dois lugares.
- `db/init.sql` (2026-08-03, movido de `tests/Fiap.Workshop.IntegrationTests/sql/init.sql`): schema completo (todas as tabelas, criado nesta sessão) e única fonte de verdade, usado tanto pelo `sqlserver-init` do compose normal quanto pelos testes de integração (via `<None Include="..\..\db\init.sql" Link="sql\init.sql">` no `.csproj`, copiado pro output). Precisa ser mantido manualmente em sincronia com `AppDbContext.OnModelCreating` sempre que o modelo mudar (não há geração automática a partir das migrations).

## 8. CI/CD (GitHub Actions)

- `ci-feature.yml` — dispara em push para `feature/**`: build → unit tests → integration tests (sobe `docker-compose.tests.yml`) → functional tests (projeto sem testes reais ainda, portanto sempre "passa" vazio).
- `ci-develop.yml` — mesmo pipeline de `ci-feature.yml` + job final `open-release-pr` que abre PR automático `develop → release` se a branch `release` existir.
- `ci-release.yml` — dispara em push para `release`: build → job `sonarqube-cloud` (ver abaixo).
- `codeql.yml` — análise de segurança estática (não inspecionado em detalhe).
- Não há workflow para `main` ainda.

### SonarQube Cloud (job `sonarqube-cloud`, em `ci-release.yml`) — adicionado em 2026-07-27, movido de `develop` para `release` no mesmo dia

Decisão: SonarQube Cloud (ex-SonarCloud — Sonar renomeou o produto em 2024, mesmo serviço, mesmo domínio `sonarcloud.io`), plano gratuito de repositório público, em vez de SonarQube Server self-hosted (exigiria manter um container/servidor rodando).

- **Por que só em `release` e não em `develop`/`feature/**`**: o **Free plan** do SonarQube Cloud só analisa a *main branch* configurada no projeto (que aqui é `release`, a branch principal do repo no GitHub) — tentar rodar em `develop` deu o erro "Organization is not allowed to access data from non main branches". Existe um **Free plan OSS** com branch analysis ilimitado pra organizações open source (repo é público, se qualificaria via `Administration → Billing` no SonarQube Cloud), mas decidiu-se não perseguir isso agora e só rodar a análise quando o código chega em `release`.
- **Escopo da análise**: restrito a `src/Fiap.Workshop.Domain/**` e `src/Fiap.Workshop.Application/**` via `/d:sonar.inclusions=...` no `dotnet sonarscanner begin` — Infrastructure/Api/tests ficam fora de propósito, já que o objetivo é vigiar a qualidade das camadas de regra de negócio (alinhado ao requisito de SAST/cobertura do Tech Challenge, ver §9 item 7).
- **Cobertura**: gerada com `coverlet.console` (global tool) envolvendo `dotnet test` do projeto `Fiap.Workshop.UnitTests` (é o projeto que cobre Domain e Application isoladamente, ver §1), formato OpenCover, reportado via `/d:sonar.cs.opencover.reportsPaths="coverage.xml"`. Integration/Functional tests não entram na cobertura reportada ao Sonar.
- **Projeto no SonarQube Cloud**: criado com "Use existing CI configuration" (não "Automatic Analysis") — necessário pra manter o `sonar.inclusions` funcionando; análise automática do Sonar ignora esse tipo de configuração custom.
- **Project key** (`/k:`): `HumbertoVitalino_fiap-postgraduated-workshop`; **organization** (`/o:`): `humbertovitalino` — hardcoded no step `Begin analysis`, não são secrets (só o token é sensível).
- **Secret necessário**: `SONAR_TOKEN` (GitHub → Settings → Secrets and variables → Actions), gerado em SonarQube Cloud (`My Account → Security`). Sem esse secret o job falha no `begin`/`end`.
- Requer `actions/setup-java` (JDK 17, Temurin) além do `.NET 10.x` já usado no resto da pipeline, porque o `dotnet-sonarscanner` roda sobre JVM.

## 9. Pendências conhecidas

> ⚠️ Lista revisada em 2026-08-15 contra o código real (branch `feature/18-get-service-order`) — login, `GetCustomer`/`GetUsers`, `UpdateUser`, `ChangePassword`, `DeleteUser` (CRUD de `User` completo), `UpdateCustomer`, `CreateVehicle`/`GetVehicle` (primeiro agregado de negócio além de `User`/`Customer` com pilha completa), `CreateService`/`GetServices` (terceiro agregado, primeiro sem referência cruzada a outro agregado), `CreateInventoryItem`/`GetInventoryItems` (quarto agregado, terceiro a ganhar invariante validada no construtor) e agora `CreateServiceOrder`/`GetServiceOrder` (quinto agregado, primeiro use case de `ServiceOrder`, abre a máquina de estados) já existem, mas o problema de bootstrap de Admin continua sem solução.

1. **Bootstrap de Admin (ainda sem solução)**: `POST /users` exige `RequireAuthorization("AdminOnly")`, sem `AllowAnonymous`, e não há seed de usuário Admin em `db/init.sql`. O endpoint de login (`POST /api/v1/auth/login`) já existe (desde a última reescrita) e funciona para autenticar um usuário **que já existe**, mas não resolve o problema original: **ainda não há nenhum caminho pela API para criar o primeiro usuário** (login pressupõe um usuário previamente criado por outro meio — direto no banco, hoje). Precisa de uma decisão: seed de Admin no `init.sql`, endpoint de bootstrap protegido por secret, ou tornar `CreateUser` público e restringir `Role: Admin` na validação/regra de negócio.
2. ~~`GetResult<Guid>()` no endpoint `CreateCustomer` provavelmente quebra em runtime`~~ — **não reproduz mais no código atual** (revisado em 2026-08-12): tanto `CustomersEndpoints.MapPost` quanto `UsersEndpoints.MapPost` usam `result.GetResult<CustomerResponse>()?.Id` / `result.GetResult<UserResponse>()?.Id`, com o tipo certo — não `GetResult<Guid>()`. Ou já foi corrigido silenciosamente em algum commit anterior, ou a descrição anterior deste documento nunca bateu com o código; de qualquer forma, hoje não é um problema. Continua valendo o ponto de fundo: `Fiap.Workshop.FunctionalTests` segue vazio (ver item 5), então esse tipo de bug de composição HTTP não tem uma camada de teste dedicada pra pegá-lo caso volte a acontecer.
3. **`.env` versionado no git** com segredos de exemplo (SA_PASSWORD, Jwt SecretKey) — confirmar se é intencional para o workshop ou se deveria ir para `.gitignore`.
4. **Mocking duplicado nos testes**: `Fiap.Workshop.UnitTests.csproj` referencia tanto `Moq` quanto `NSubstitute`; os testes atuais usam apenas `Moq`. Vale decidir um padrão único.
5. **`Fiap.Workshop.FunctionalTests`** existe como projeto mas não tem nenhum arquivo de teste ainda — scaffold vazio. Sem ele, bugs de composição HTTP (ver item 2) não são pegos por nenhuma camada de teste.
6. **Domain events sem consumidor**: mecânica pronta e correta (ver §11), mas nenhum `IDomainEventHandler` está registrado no DI — o dispatch acontece e não aciona nada. E-mail de boas-vindas planejado como primeiro handler, ver roadmap em §11. `Customer` nem dispara evento próprio ainda (ver §3).
7. **Escopo do Tech Challenge (Fase 1) — progresso desde a última passada**: o enunciado (`15SOAT - Fase 1 - Tech Challenge (1).pdf`, na raiz) pede um sistema de oficina mecânica — CRUD de clientes/veículos/serviços/peças (com controle de estoque), Ordem de Serviço com máquina de estados, orçamento automático, validação de CPF/CNPJ e placa, cobertura de teste mínima de 80% nos domínios críticos, relatório de vulnerabilidades (SAST) e documentação DDD.
   - **Cliente**: tem criação, consulta por id e agora atualização (`CreateCustomer` + `GetCustomer` + `UpdateCustomer` — Domain→Application→Infrastructure→Api, ver §3/§4/§6), incluindo validação de CPF/CNPJ (dígito verificador via `CpfCnpjLibrary`, ver §2/§6). `UpdateCustomer` deixa o `Document` imutável por decisão de negócio (ver §3) — só `Name`/`Email`/`Phone` são editáveis. Ainda falta listagem e remoção de cliente — quando a remoção for implementada, `DeleteUser` (§4/§6, `feature/9-user-delete`) é o template a seguir: use case idempotente (não encontrado = sucesso, não erro) + endpoint `204 No Content`/`400 BadRequest`, em vez do padrão `404`/`400` usado hoje em `GetCustomer`.
   - **Veículo**: tem criação e consulta por id (`CreateVehicle` + `GetVehicle` — Domain→Application→Infrastructure→Api, ver §3/§4/§6), incluindo validação de formato de placa (Mercosul/antigo, ver §2/§6) e a invariante `ModelYear >= ManufactureYear` no domínio (primeira validação de construtor do projeto exercitada por um use case real, ver §3). Ainda falta listagem, atualização e remoção de veículo.
   - **Serviço**: tem criação e listagem (`CreateService` + `GetServices` — Domain→Application→Infrastructure→Api, ver §3/§4/§6), com unicidade de `Code` e a invariante `BasePrice`/`EstimatedDuration >= 0` no domínio (segunda validação de construtor do projeto exercitada por um use case real, depois de `Vehicle`). `GET /services` é acessível pelos três roles (`.RequireAuthorization()` sem policy nomeada), diferente do `POST` (`AdminOnly`). Ainda falta atualização/remoção de serviço (não estão no backlog atual, ver `docs/backlog-proximas-tarefas.md`).
   - **Peça/Insumo**: tem criação e listagem (`CreateInventoryItem` + `GetInventoryItems` — Domain→Application→Infrastructure→Api, ver §3/§4/§6), com unicidade de `Code` e as invariantes `QuantityOnHand`/`ReservedQuantity`/`MinimumStock`/`UnitPrice >= 0` + `ReservedQuantity <= QuantityOnHand` no domínio (terceira validação de construtor do projeto, depois de `Vehicle`/`Service`). `GET /inventory-items` é acessível pelos três roles (`.RequireAuthorization()` sem policy nomeada), mesmo padrão do `GET /services`. Com isso já dá pra consultar o estoque disponível pra decidir se um pedido de orçamento (ticket 20, `AddBudget`) cabe. Ainda falta atualização/remoção de item (não estão no backlog atual).
   - **Ordem de Serviço**: tem abertura e consulta por id (`CreateServiceOrder` + `GetServiceOrder` — Domain→Application→Infrastructure→Api, ver §3/§4/§6), validando cliente existente, veículo existente **e** veículo pertencente ao cliente informado na criação (três motivos de `400`, além de erro de validação de campo). Nasce em `Received`, sem orçamento (`Discount`/`Subtotal`/`Total = 0`, `Parts`/`Services`/`StatusHistory` vazios). `POST /service-orders` é `AttendantOnly`; `GET /service-orders/{id}` é `.RequireAuthorization()` sem policy nomeada (os três papéis atuam na OS em pontos diferentes do fluxo). **Campo `Number` (identificador curto tipo "OS 1042") foi cogitado e descartado** em `feature/17-create-service-order` — ver §3 pra decisão completa; `ServiceOrder` usa só `Id` (Guid). `GetServiceOrder` devolve `Parts`/`Services`/`StatusHistory` no mesmo `ServiceOrderResponse` do `CreateServiceOrder` (não um DTO separado, ver §4). **Gap de Infra descoberto no teste de integração desta feature, ainda não corrigido**: `Repository<T>.Upsert`/`ServiceOrderRepository.Update` não persistem corretamente filhos *novos* adicionados a uma OS já existente (fluxo "carregar → `AddParts`/`AddServices` → `Update()`") — ver §4/§5 pro diagnóstico completo. **Bloqueia o ticket 20 (`AddBudget`)**, que depende exatamente desse fluxo; precisa ser corrigido antes ou durante aquele ticket. Falta ainda toda a máquina de estados (`StartDiagnosis`/`AddBudget`/`Approve`/`Reject`/`Complete`/`Deliver`, tickets 19-24) — sem eles, a OS aberta hoje nunca muda de status pela API.
   - **Ordem de Serviço**: continua só com Domain e Infrastructure prontos (entidade, model, repositório, mapeamento, migration) — falta a camada de Application (use cases) e Api (endpoints) por cima dela. Ver `docs/backlog-proximas-tarefas.md` pro backlog refinado (tickets `#16` a `#24`) que cobre o restante até fechar o fluxo completo de OS.
   - **Ainda faltando por completo**: máquina de estados de OS, orçamento automático, controle de estoque, cobertura de 80%, relatório de vulnerabilidades (SAST), documentação DDD (Event Storming/diagramas/linguagem ubíqua, entregável separado do código — ver `docs/backlog-proximas-tarefas.md`).
   - Ver seção "Roadmap / pendências do desafio" do `README.md` para a lista cobrada pelo enunciado — **está desatualizada** (ainda diz que login e `Customer` não existem); vale revisar numa próxima passada.
8. **`GetUsersUseCase` devolve dois formatos diferentes em `Output.Result`** (novo, `feature/7-get-users`, ver §4): `Array.Empty<User>()` (entidade de domínio) quando a tabela está vazia, `IEnumerable<UserResponse>` (DTO) quando não está. Como `Result` é `object?`, isso não quebra a build, mas o JSON devolvido pelo `GET /api/v1/users` muda de forma dependendo do estado do banco. Simplificar para sempre devolver `users.MapToDto()` (que já lida com lista vazia sem precisar do `if`) resolve.
9. ~~`ChangePasswordUseCase` existe sem endpoint Api ainda~~ — **resolvido em 2026-08-12**: `PATCH /api/v1/users/{id}/password` implementado (ver §6), com a checagem de identidade (`ICurrentUserService.UserId == userId` da rota) que faz da rota um self-service de verdade.
10. **Troca de senha não revoga tokens JWT já emitidos** (novo, `feature/8-put-users`, ver §4): decisão consciente de escopo, não descuido — o `JwtService` é stateless (sem `jti`, sem sessão, sem denylist), então revogar um token específico exigiria uma mudança estrutural na autenticação (`TokenVersion`/`SecurityStamp` + validação customizada no pipeline JWT, ou denylist, ou refresh tokens). Registrado aqui pra não se perder: se a troca de senha precisar "deslogar" o usuário de outros dispositivos no futuro, é esse o ponto de partida.
11. **A checagem de autorização do `PATCH /users/{id}/password` (`403` se `userId` da rota não for o do chamador) não tem cobertura de teste nenhuma** (novo, `feature/8-put-users`, ver §6): diferente da lógica de negócio de `ChangePasswordUseCase` (testada em 3 camadas), essa regra vive só no lambda do endpoint na Api, e como `Fiap.Workshop.FunctionalTests` continua vazio (item 5), nenhum teste automatizado passa por ela — nem foi verificada manualmente ainda, porque isso esbarra na mesma pendência do bootstrap de Admin (item 1): sem um jeito de emitir um JWT de teste, não dá pra bater na rota de verdade. É a parte mais sensível do que foi construído em `feature/8-put-users` e a única sem nenhuma verificação.
12. ~~Ideia discutida, não implementada: `Service.EstimatedDuration` como média calculada em vez de valor estático~~ — **decisão fechada em 2026-08-16, retomada exatamente como planejado quando chegou a vez do ticket 23** (proposta originalmente discutida em 2026-08-15, `feature/14-get-services`, e adiada de propósito até este ponto). Motivo pra retomar agora: bate melhor com a redação literal do PDF ("monitoramento do tempo médio de execução **dos serviços**", plural — por tipo de serviço do catálogo, não por OS inteira) do que a métrica calculada por `StatusHistory` que tinha sido cogitada primeiro (`InProgress → Completed` por OS, ver nota do ticket 31 removido em `docs/backlog-proximas-tarefas.md`).
    - **Ressalva levantada antes de o usuário decidir, registrada aqui pra não se perder**: um campo chamado *Estimated* sendo silenciosamente sobrescrito pela média de execuções reais perde clareza semântica (deixa de significar só "estimativa inicial" do catálogo), e é uma mutação de dado `AdminOnly` (`Service`) disparada por um fluxo `MechanicOnly` (`CompleteServiceOrder`). Alternativa proposta (manter separado, relatório calculado à parte) foi rejeitada pelo usuário — decisão final foi manter a média incremental direto no campo mesmo assim, mitigada com o `ExecutionCount` exposto na API (ver abaixo), pra quem consome saber quantas amostras embasam o número.
    - **Desenho fechado**: `Service.RecordExecution(short actualDuration)` — média incremental (`novo = antigo + (valor - antigo) / (contagem + 1)`), com um contador `ExecutionCount` (`int`, novo campo) guardando quantas amostras já entraram na média. Se um admin editar `EstimatedDuration` manualmente via `UpdateService` (ticket 28), isso reseta `ExecutionCount` pra `0` — o valor definido vira um novo ponto de partida, não mais um ponto diluído entre correção manual e amostras antigas.
    - **Dependências (ver ticket 23 em `docs/backlog-proximas-tarefas.md` pro desenho completo)**: campo novo `ActualDuration` (`short?`) em `ServiceOrderService`, obrigatório informar pra **todos** os serviços da OS ao concluir (`ServiceOrder.Complete` passa a receber esse payload, valida que nenhum está faltando); `CompleteServiceOrderUseCase` orquestra dois agregados (`ServiceOrder` + `Service`, cada `Service` tocado chama `RecordExecution`) — mesmo padrão de orquestração cross-aggregate já usado em `AddBudgetUseCase`/`ApproveServiceOrderUseCase`.
    - Ver também a nota sobre unidade de `EstimatedDuration` (convenção implícita de minutos) em §3 — `ActualDuration` segue a mesma unidade, senão a comparação/média não faz sentido.

## 10. Senha em `User` — desenho atual (reescrito em 2026-08-08)

> Esta seção descrevia anteriormente um desenho DDD (VO `HashedPassword`, `IPasswordHasher` como Domain Service, fábricas `Create`/`Rehydrate`) concluído numa iteração anterior do projeto. **Esse desenho não está presente no código desta branch** (`feature/3-create-user-uc`) — o que segue é o que existe de fato hoje.

- `User.Password` é uma `string` comum — sem Value Object, sem política de força aplicada no Domain (`UserErrors.PasswordTooShort`/`PasswordMissingUppercase`/etc. existem mas não são referenciadas por nenhum código).
- `IPasswordService` (não `IPasswordHasher`) mora em `Application.Interfaces.Services` — hashear senha é tratado como serviço técnico, não como invariante do agregado `User`. Implementação: `PasswordService` (Infrastructure), `BCrypt.Net-Next`, work factor 12, registrada como `Singleton`.
- `User` não hasheia nem valida a própria senha. Quem orquestra é `CreateUserMapper.MapToDomain` (Application): recebe o hash já pronto (`IPasswordService.Hash(input.Password)` calculado no use case) e só então constrói o `User`.
- Não existem `Create`/`Rehydrate`: o único construtor de `User` serve tanto para criar quanto para reidratar do banco, e sempre dispara `UserCreatedEvent` (ver §3).
- Validação de senha hoje só existe na Api (`CreateUserRequestValidator`/`LoginRequestValidator`: `NotEmpty().MinimumLength(8)`, sem checar maiúscula/minúscula/dígito) — não há "defesa em profundidade" replicada no Domain como a antiga versão deste documento descrevia.
- **`LoginUserUseCase` já existe** (desde a última reescrita deste documento, ver §4/§6) e usa `IPasswordService.Verify` para checar a senha contra o hash guardado — `LoginResponse` (DTO) segue existindo mas não é usado; o use case devolve o token JWT como `string` cru.

Se o time decidir reintroduzir Value Objects/fábricas nomeadas para `User`, é um redesenho consciente a ser feito — não é o estado atual.

## 11. Domain Events — mecânica atual (importante entender antes de adicionar handlers)

> ⚠️ Ajustada em 2026-08-08: a versão anterior descrevia um bug no `CommitAsync` que **já foi corrigido** no código atual, e referenciava `AggregateRoot<TId>`/`User.Create`, que não existem mais (ver §3).

Como funciona hoje, de ponta a ponta:

1. O construtor de `User(...)` chama `RaiseDomainEvent(new UserCreatedEvent(id))` — isso só adiciona o evento a uma `List<IDomainEvent>` **em memória**, dentro do próprio objeto `User` (`AggregateRoot._domainEvents`). Isso acontece **toda vez** que um `User` é instanciado — criação de verdade (`CreateUserUseCase`) ou reidratação a partir do banco (`DomainMappers.MapToDomain`) — não existe um caminho separado para "isso já existia" (ver §3).
2. `UserRepository.AddAsync`/`Update` movem esses eventos para dentro do `AppDbContext` (`EnqueueDomainEvents`) e limpam a lista do `User` (`EnqueueEvents`, em `Repository<T>`). **Leituras (`GetByIdAsync`/`GetByEmailAsync`) não chamam `EnqueueEvents`** — o `UserCreatedEvent` gerado ao reidratar um usuário existente é descartado junto com o objeto, nunca chega a ser enfileirado. Frágil (ver §3), mas inofensivo hoje.
3. `AppDbContext.CommitAsync` roda `SaveChangesAsync` num `try/catch` **próprio** (grava o `UserModel` no SQL Server; se falhar, loga e retorna `false` direto). **Só depois**, num `try/catch` **separado**, entrega os eventos pendentes ao `IDomainEventDispatcher.DispatchAsync` — uma falha aqui só é logada, não afeta o `bool` retornado.
4. `DomainEventDispatcher` resolve via reflection quais `IDomainEventHandler<TEvent>` estão registrados no DI e invoca `HandleAsync` em cada um, **de forma síncrona**, ainda dentro da mesma requisição/antes de responder ao cliente.

**Conclusões importantes:**
- **Eventos de domínio NÃO são persistidos em lugar nenhum.** Não existe tabela de eventos, fila ou log. É puramente um mecanismo de notificação em memória, vivo só durante aquela unit of work.
- Isso **não é Event Sourcing** (o estado não é reconstruído replayando eventos — vive direto na tabela `Users`) nem **Transactional Outbox** (não há garantia de entrega; se o processo cair entre o `SaveChangesAsync` e o `DispatchAsync`, o evento se perde silenciosamente).
- **Hoje não existe nenhum `IDomainEventHandler` registrado no DI** — o dispatch acontece mas não aciona nada. Toda a infraestrutura existe, mas está inerte.
- **O bug de "falha no dispatch derruba o resultado do commit", descrito numa versão anterior deste documento, já está corrigido**: `SaveChangesAsync` e `DispatchAsync` têm `try/catch` independentes (passo 3 acima) — não é mais um pré-requisito bloqueando a adição de handlers.

### Plano para a próxima sessão: e-mail de boas-vindas ao criar usuário

Discutido e ainda não implementado — retomar aqui (já foi prototipado uma vez como exercício de compreensão e revertido de propósito; o desenho abaixo continua válido, com o item 1 original já resolvido):

1. ~~Corrigir o bug do `CommitAsync`~~ — **já corrigido** (ver acima), não bloqueia mais nada.
2. **Enriquecer `UserCreatedEvent`**: hoje só tem `UserId`; para mandar e-mail é preciso `Email`/`Name`. Decisão tomada: preferir "evento gordo" (`UserCreatedEvent(Guid UserId, string Email, string Name)`) em vez de o handler re-consultar o `IUserRepository` — mais barato e captura o estado exatamente como era no momento da criação.
3. **`IEmailService`** — nova interface em `Application/Interfaces/Services/`, algo como `SendWelcomeEmailAsync(string email, string name)`.
4. **Implementação em Infrastructure**: como não há provedor de e-mail configurado (`.env` não tem SMTP/SendGrid), começar com um `LoggingEmailService` que só loga o envio — ponto de extensão pronto para um provedor real depois.
5. **Handler**: `SendWelcomeEmailOnUserCreated : IDomainEventHandler<UserCreatedEvent>` — colocar em **Application** (não Infrastructure), já que só orquestra uma chamada a uma abstração (`IEmailService`), sem detalhe técnico nenhum. Registrar no DI de `AddApplication()`.
6. Ponderar (não decidido ainda): o dispatch síncrono faz o `POST /users` esperar o "envio" do e-mail antes de responder. Para um provedor de e-mail real (chamada HTTP externa), isso adiciona latência à resposta. Para o workshop, começar síncrono é aceitável — mas vale registrar que, se algum dia isso incomodar, a evolução natural é official Outbox pattern + worker em background, não o dispatcher atual.
7. **Testar em 3 camadas**: unit test em `User` (evento carrega os dados certos), unit test no handler (mock de `IEmailService`), integration test ponta a ponta (spy de `IEmailService` sobrescrevendo `LoggingEmailService` via DI no `DatabaseFixture`, criando usuário via `ICreateUserUseCase` de verdade). Esse desenho de teste em camadas já foi validado no protótipo revertido.

## 12. `Email` em `User` — nunca foi Value Object nesta branch (nota reescrita em 2026-08-08)

> Esta seção descrevia anteriormente a remoção de um VO `Email` (`Domain/Users/Email.cs`) numa iteração anterior do projeto. Nesta branch (`feature/3-create-user-uc`), `Email` **nunca existiu como Value Object** — sempre foi `string` simples em `User`, sem normalização (sem `Trim()`/`ToLowerInvariant()` em nenhum lugar do código atual). Registrado aqui só para não perder o contexto de que essa decisão (VO vs. string simples) já foi discutida antes; ver §10 para o desenho atual completo de `User`.

Validação de formato/tamanho de e-mail hoje só existe em `CreateUserRequestValidator` (Api): `NotEmpty().EmailAddress()`, sem limite de tamanho explícito (o `MaxLength(256)` é só na coluna do banco, ver §5). Duplicidade é garantida pelo índice único `IX_Users_Email` no SQL Server + `ExistsWithEmailAsync` checado antes do insert — mas como não há normalização, `"joao@x.com"` e `"JOAO@x.com"` são tratados como e-mails diferentes hoje.
